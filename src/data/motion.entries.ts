import type { ClusterEntry } from "@/lib/cluster";

/**
 * AUTO-ASSEMBLED AI-motion / pose-estimation pages (do not hand-edit; regenerate
 * via scratchpad/assemble13.mjs). Article + FAQPage (no HowTo). The technical /
 * decision layer — which model, 2D vs 3D, on-device vs cloud, how rep-counting
 * and form-scoring work — linking to /guides (how-to) and /learn (concept).
 * Placeholder until the writers land.
 */
export const motionEntries: ClusterEntry[] = [];
