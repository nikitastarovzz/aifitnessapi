/**
 * Cluster 17 — the Cookbook: runnable, CI-tested reference implementations
 * of the patterns the site documents. BOUNDARY RULE: a recipe page explains
 * briefly and links the theory pages; the code is the content. Every fenced
 * code block must be a byte-verbatim copy of the corresponding file in
 * /cookbook/ at the repo root — those files run under `npm run
 * test:cookbook` in CI (.github/workflows/cookbook-ci.yml), and the pages'
 * "CI-tested" claim depends on that staying true. Zero-dependency .mjs,
 * node:test, injectable everything. Hand-editing entries is expected.
 */
import type { ClusterEntry } from "@/lib/cluster";

export const cookbookEntries: ClusterEntry[] = [
  {
    "slug": "refresh-rotation",
    "primaryQuery": "oauth refresh token rotation code example",
    "h1": "Recipe: Single-Flight Refresh-Token Rotation",
    "metaTitle": "Recipe: Single-Flight Refresh Token Rotation",
    "metaDescription": "A dependency-free, tested token client: one refresh per user, the rotated token persisted atomically, one retry on 401, a dead grant on invalid_grant.",
    "updated": "2026-08-12",
    "answer": "Refresh-token rotation breaks integrations in four predictable ways: concurrent refreshes race each other into invalid_grant, the returned refresh token is not persisted, a 401 retry loop hammers the provider, and a dead grant gets retried forever. This recipe is a small JavaScript token client that closes all four — refresh is single-flight per user, both tokens are written in one atomic save that is awaited before the promise resolves, a 401 buys exactly one refresh and one retry, and invalid_grant marks the grant dead instead of retrying. The store, the clock and fetch are all injected, so it runs in tests without a network. Copy it, swap the store for your database, and keep the tests.",
    "faqs": [
      {
        "q": "Is this recipe safe to paste into a production backend?",
        "a": "It is written to be, with one caveat about scale. The logic — per-user single flight, atomic persistence, bounded retry, dead-grant terminal state — is what a correct implementation needs, and the test file holds it to that contract on every CI run. The caveat is that the in-flight lock is a Map inside one process, so it serializes refreshes within a single worker only. If several processes or pods can refresh the same user at the same time, keep this shape but back the lock with something shared, such as a Postgres advisory lock or a Redis lock keyed by user id."
      },
      {
        "q": "How would I plug my own database into the grant store?",
        "a": "Implement two methods: load(userId), returning the stored record or null, and save(userId, record), writing the whole record in one statement. The single hard requirement is that save be atomic — one UPDATE setting the access token, the refresh token and the expiry together. A store that writes the access token in one statement and the refresh token in another recreates the exact bug this recipe exists to prevent, because a crash between the two leaves you holding a refresh token the provider has already invalidated."
      },
      {
        "q": "What does the client do when the token endpoint returns 503 rather than invalid_grant?",
        "a": "It raises a retryable error and leaves the stored grant completely untouched. That distinction is deliberate: invalid_grant means the credential is gone and no retry can help, so the grant is marked dead and the tokens are cleared. Anything else — a 5xx, a proxy hiccup, invalid_client — is transient from the grant's point of view. Clearing a good refresh token on an outage converts a bad afternoon into a re-authorization campaign across your whole user base."
      },
      {
        "q": "Does the recipe still work with a provider that does not rotate refresh tokens?",
        "a": "Yes, and that is the point of writing it this way. When the token response omits refresh_token, the client keeps the one it already had, so the same code path serves rotating and non-rotating providers. Treating every refresh token as single-use is the rule that is safe everywhere: it costs nothing against a provider that returns the same value, and it is the only correct behaviour against one that does not."
      }
    ],
    "related": [
      {
        "href": "/fix/refresh-token-not-working",
        "label": "Fix: refresh token not working"
      },
      {
        "href": "/fix/strava-api-401-unauthorized",
        "label": "Fix: Strava API 401 Unauthorized"
      },
      {
        "href": "/learn/what-are-oauth-scopes",
        "label": "What OAuth scopes are, and what they are not"
      },
      {
        "href": "/integrate/strava-api",
        "label": "Strava API integration guide"
      }
    ],
    "cta": {
      "pitch": "Every recipe in the cookbook starts as a bug we watched a real integration hit. Subscribe and get the next one before it costs you an afternoon."
    },
    "body": "Rotation is the failure mode that takes out every user at once. Your refresh loop runs, the provider hands back a new refresh token, your code keeps the old one, and from that moment nothing can mint a working access token for anybody. The status page says the provider is fine. It usually is.\n\n## The four things the code has to get right\n\nThe full triage lives in [refresh token not working](/fix/refresh-token-not-working), and the provider-specific version — including the athlete-deauthorization signal — is in [Strava API 401 Unauthorized](/fix/strava-api-401-unauthorized). The compressed version, because the code is the point of this page:\n\n- **Take the returned refresh token, every time.** Strava, WHOOP, Oura, Garmin and Fitbit all invalidate the old one immediately. Miss the returned value once and every later refresh answers `400 invalid_grant`.\n- **Serialize refreshes per user.** Two refreshes racing for the same person rotate the token out from under each other, so a codebase whose persistence is perfectly correct still throws intermittent `invalid_grant` under load.\n- **Bound the 401 retry.** A 401 that triggers a refresh that triggers a retry that 401s again is a loop, and it is your own denial-of-service pointed at the provider.\n- **Treat `invalid_grant` as terminal.** A dead grant cannot be retried into life. Every attempt after the first is work that can never succeed.\n\nOne rule sits alongside those and is not in the code path at all: a `403` is never refreshed. It means the token is authentic and the scope is missing, and a refresh mints the same scopes the user already granted — see [what OAuth scopes are](/learn/what-are-oauth-scopes) for why the granted set is rarely the requested set. The cross-provider split between the two status codes is in [fitness API 401 Unauthorized](/fix/fitness-api-401-unauthorized).\n\n## The implementation\n\nThe canonical file is `cookbook/refresh-rotation.mjs` in this site's repository, and `cookbook/refresh-rotation.test.mjs` runs against it on every CI run. The listing below is a verbatim copy of that file. It has no runtime dependencies, needs Node 20 or newer, and takes its store, its clock and its `fetch` as arguments, so it runs in a test without a network.\n\n```js\n/**\n * refresh-rotation.mjs — a rotation-safe OAuth token client for fitness APIs.\n *\n * WHAT THIS IMPLEMENTS\n *   Providers that rotate refresh tokens (Strava, WHOOP, Oura, Garmin, Fitbit)\n *   invalidate the old refresh token the moment they hand you a new one. Four\n *   disciplines keep that from breaking every user at once:\n *     1. Refresh is single-flight PER USER — concurrent callers await one\n *        token-endpoint call instead of racing each other into `invalid_grant`.\n *     2. The returned refresh token is persisted ATOMICALLY with the access\n *        token, and the write is awaited before the refresh promise resolves.\n *     3. A 401 on an API call triggers exactly one refresh + retry. A second\n *        401 raises instead of looping.\n *     4. A refresh that fails with `invalid_grant` marks the grant DEAD\n *        (re-authorization required) rather than retrying forever. Every other\n *        refresh failure is transient and leaves the stored grant untouched.\n *   A 403 is never refreshed: it means the token is authentic but the scope is\n *   missing, and a refresh mints the same scopes the user already granted.\n *\n * PATTERN DOCUMENTED AT\n *   https://aifitnessapi.com/fix/refresh-token-not-working\n *   https://aifitnessapi.com/fix/strava-api-401-unauthorized\n *   https://aifitnessapi.com/learn/what-are-oauth-scopes\n *\n * Everything is injectable (store, fetch, clock) so this file is testable\n * without a network or a real provider. No runtime dependencies. Node 20+.\n *\n * MIT — from aifitnessapi.com/cookbook\n */\n\n/** HTTP methods aside, this is the only content type OAuth token endpoints take. */\nconst FORM_URLENCODED = \"application/x-www-form-urlencoded\";\n\n/** Default proactive-refresh buffer: refresh when < 5 minutes of life remain. */\nconst DEFAULT_REFRESH_SKEW_MS = 5 * 60 * 1000;\n\n/**\n * The grant is gone: revoked, expired, or rotated out from under us. Retrying\n * cannot fix this — the user has to authorize again.\n */\nexport class DeadGrantError extends Error {\n  constructor(userId, reason, detail) {\n    super(`grant for user ${userId} is dead (${reason}) — re-authorization required`);\n    this.name = \"DeadGrantError\";\n    this.userId = userId;\n    this.reason = reason;\n    this.detail = detail;\n    /** Callers branch on this to route the user back through authorize. */\n    this.requiresReauth = true;\n  }\n}\n\n/** The token endpoint failed in a way that may succeed later. Grant untouched. */\nexport class RefreshFailedError extends Error {\n  constructor(userId, status, payload) {\n    super(`refresh for user ${userId} failed with status ${status}`);\n    this.name = \"RefreshFailedError\";\n    this.userId = userId;\n    this.status = status;\n    this.payload = payload;\n    this.retryable = true;\n  }\n}\n\n/** Refreshed once, retried once, still 401. Something else is wrong — stop. */\nexport class UnauthorizedAfterRefreshError extends Error {\n  constructor(userId, url) {\n    super(`user ${userId} still 401 after one refresh+retry of ${url}`);\n    this.name = \"UnauthorizedAfterRefreshError\";\n    this.userId = userId;\n    this.url = url;\n    this.status = 401;\n  }\n}\n\n/**\n * Granted scope comes back space-delimited from most providers and\n * comma-delimited from Strava. Accept both rather than shipping two parsers.\n */\nexport function parseScope(value) {\n  if (!value) return [];\n  if (Array.isArray(value)) return value.slice();\n  return String(value)\n    .split(/[\\s,]+/)\n    .filter(Boolean);\n}\n\nfunction isOk(res) {\n  if (typeof res?.ok === \"boolean\") return res.ok;\n  return res?.status >= 200 && res?.status < 300;\n}\n\nasync function readJson(res) {\n  try {\n    return await res.json();\n  } catch {\n    return null;\n  }\n}\n\n/**\n * Providers disagree on how they express expiry. Strava sends `expires_at` in\n * epoch SECONDS; most others send `expires_in` in seconds. Normalize to epoch ms.\n */\nfunction expiryFromPayload(payload, nowMs, defaultTtlSec) {\n  const at = payload?.expires_at;\n  if (typeof at === \"number\" && Number.isFinite(at)) {\n    // Below ~1e11 the value cannot be milliseconds this century, so it is seconds.\n    return at < 1e11 ? at * 1000 : at;\n  }\n  const inSec = Number(payload?.expires_in);\n  if (Number.isFinite(inSec) && inSec > 0) return nowMs + inSec * 1000;\n  return nowMs + defaultTtlSec * 1000;\n}\n\n/**\n * @typedef {object} GrantRecord\n * @property {string|null} accessToken\n * @property {string|null} refreshToken\n * @property {number} expiresAt        epoch ms\n * @property {string[]} [scope]        granted scope, as returned by the provider\n * @property {\"active\"|\"dead\"} status\n * @property {string|null} [deadReason]\n */\n\n/**\n * @typedef {object} GrantStore\n * @property {(userId: string) => Promise<GrantRecord|null>} load\n * @property {(userId: string, record: GrantRecord) => Promise<void>} save\n *   MUST write the whole record in one atomic operation. A store that writes\n *   the access token and the refresh token in two statements recreates the bug\n *   this file exists to prevent.\n */\n\n/**\n * @param {object} options\n * @param {GrantStore} options.store\n * @param {string} options.tokenEndpoint\n * @param {string} [options.clientId]\n * @param {string} [options.clientSecret]\n * @param {typeof globalThis.fetch} [options.fetch]\n * @param {() => number} [options.now]  epoch ms; injected in tests\n * @param {number} [options.refreshSkewMs]\n * @param {number} [options.defaultTtlSec]\n */\nexport function createRotatingTokenClient({\n  store,\n  tokenEndpoint,\n  clientId,\n  clientSecret,\n  fetch: fetchImpl = globalThis.fetch,\n  now = () => Date.now(),\n  refreshSkewMs = DEFAULT_REFRESH_SKEW_MS,\n  defaultTtlSec = 3600,\n} = {}) {\n  if (!store || typeof store.load !== \"function\" || typeof store.save !== \"function\") {\n    throw new TypeError(\"store with load()/save() is required\");\n  }\n  if (!tokenEndpoint) throw new TypeError(\"tokenEndpoint is required\");\n  if (typeof fetchImpl !== \"function\") throw new TypeError(\"fetch is required\");\n\n  /** userId -> in-flight refresh promise. This map IS the per-user lock. */\n  const inFlight = new Map();\n\n  async function requireLiveGrant(userId) {\n    const record = await store.load(userId);\n    if (!record) throw new DeadGrantError(userId, \"no_grant\");\n    if (record.status === \"dead\") {\n      throw new DeadGrantError(userId, record.deadReason || \"revoked\");\n    }\n    return record;\n  }\n\n  async function performRefresh(userId) {\n    const record = await requireLiveGrant(userId);\n    if (!record.refreshToken) {\n      // No refresh token was ever issued — usually a missing offline-access\n      // scope (WHOOP's `offline`). Re-authorize; there is nothing to refresh.\n      throw new DeadGrantError(userId, \"no_refresh_token\");\n    }\n\n    const body = new URLSearchParams({\n      grant_type: \"refresh_token\",\n      refresh_token: record.refreshToken,\n    });\n    if (clientId) body.set(\"client_id\", clientId);\n    if (clientSecret) body.set(\"client_secret\", clientSecret);\n\n    const res = await fetchImpl(tokenEndpoint, {\n      method: \"POST\",\n      headers: { \"content-type\": FORM_URLENCODED, accept: \"application/json\" },\n      body: body.toString(),\n    });\n    const payload = await readJson(res);\n\n    if (!isOk(res)) {\n      if (payload?.error === \"invalid_grant\") {\n        // The grant is dead. Clear the tokens in the SAME atomic write that\n        // sets the dead flag, so a crash cannot leave a half-dead record that\n        // some other worker will keep retrying.\n        await store.save(userId, {\n          ...record,\n          accessToken: null,\n          refreshToken: null,\n          expiresAt: 0,\n          status: \"dead\",\n          deadReason: \"invalid_grant\",\n          deadAt: now(),\n        });\n        throw new DeadGrantError(userId, \"invalid_grant\", payload?.error_description);\n      }\n      // Anything else (5xx, invalid_client, a proxy hiccup) is transient from\n      // the grant's point of view. Do NOT touch the stored refresh token:\n      // deleting a good token on a 503 turns an outage into a re-auth campaign.\n      throw new RefreshFailedError(userId, res?.status ?? 0, payload);\n    }\n\n    if (!payload?.access_token) {\n      throw new RefreshFailedError(userId, res?.status ?? 200, payload);\n    }\n\n    const next = {\n      ...record,\n      accessToken: payload.access_token,\n      // ALWAYS take the returned refresh token. Strava's docs: \"the refresh\n      // token may or may not be the same refresh token used to make the\n      // request... always use the most recent refresh token.\"\n      refreshToken: payload.refresh_token ?? record.refreshToken,\n      expiresAt: expiryFromPayload(payload, now(), defaultTtlSec),\n      scope: payload.scope ? parseScope(payload.scope) : record.scope,\n      status: \"active\",\n      deadReason: null,\n      rotatedAt: now(),\n    };\n\n    // One atomic write, awaited. The refresh promise does not resolve until the\n    // new refresh token is durable — otherwise a caller could use the new\n    // access token, crash, and come back holding a refresh token the provider\n    // has already invalidated.\n    await store.save(userId, next);\n    return next;\n  }\n\n  /**\n   * Single-flight refresh, keyed by user. Twenty workers that all notice an\n   * expired token at the same instant produce exactly one token-endpoint call.\n   */\n  function refresh(userId) {\n    const existing = inFlight.get(userId);\n    if (existing) return existing;\n\n    const promise = (async () => {\n      try {\n        return await performRefresh(userId);\n      } finally {\n        if (inFlight.get(userId) === promise) inFlight.delete(userId);\n      }\n    })();\n    inFlight.set(userId, promise);\n    return promise;\n  }\n\n  /**\n   * Returns a usable access token, refreshing proactively when less than\n   * `minTtlMs` of life remains. Never waits for a 401 to find out.\n   */\n  async function getAccessToken(userId, { minTtlMs = refreshSkewMs } = {}) {\n    const record = await requireLiveGrant(userId);\n    const ttl = (record.expiresAt ?? 0) - now();\n    if (record.accessToken && ttl > minTtlMs) return record.accessToken;\n    const next = await refresh(userId);\n    return next.accessToken;\n  }\n\n  async function callOnce(userId, url, init) {\n    const token = await getAccessToken(userId);\n    const res = await fetchImpl(url, {\n      ...init,\n      headers: { ...(init?.headers ?? {}), authorization: `Bearer ${token}` },\n    });\n    return { res, token };\n  }\n\n  /**\n   * If another worker already rotated while our request was in flight, use the\n   * token it stored instead of burning a second rotation. This is what keeps a\n   * burst of 401s from turning into a burst of refreshes.\n   */\n  async function refreshAfterUnauthorized(userId, usedToken) {\n    const current = await store.load(userId);\n    if (current && current.status !== \"dead\" && current.accessToken && current.accessToken !== usedToken) {\n      return current;\n    }\n    return refresh(userId);\n  }\n\n  /**\n   * Authenticated fetch with exactly one refresh + retry on 401.\n   *\n   * 403 is deliberately NOT retried: it means `insufficient_scope`, and a\n   * refresh returns the same scopes the user already granted, so retrying is\n   * an infinite loop against a server that is answering correctly.\n   */\n  async function fetchWithAuth(userId, url, init = {}) {\n    const first = await callOnce(userId, url, init);\n    if (first.res.status !== 401) return first.res;\n\n    await refreshAfterUnauthorized(userId, first.token);\n\n    const second = await callOnce(userId, url, init);\n    if (second.res.status === 401) {\n      // One refresh, one retry, done. Looping here is how a bad credential\n      // becomes a self-inflicted denial of service against the provider.\n      throw new UnauthorizedAfterRefreshError(userId, String(url));\n    }\n    return second.res;\n  }\n\n  /**\n   * Kill a grant on an out-of-band signal — e.g. a Strava `athlete` webhook\n   * with `updates.authorized === \"false\"`. Cheaper than learning from a 401.\n   */\n  async function markRevoked(userId, reason = \"revoked\") {\n    const record = (await store.load(userId)) ?? {};\n    await store.save(userId, {\n      ...record,\n      accessToken: null,\n      refreshToken: null,\n      expiresAt: 0,\n      status: \"dead\",\n      deadReason: reason,\n      deadAt: now(),\n    });\n  }\n\n  /** Read the GRANTED scope, not the requested one. Users deselect scopes. */\n  async function hasScope(userId, scope) {\n    const record = await store.load(userId);\n    return parseScope(record?.scope).includes(scope);\n  }\n\n  return {\n    getAccessToken,\n    refresh,\n    fetchWithAuth,\n    markRevoked,\n    hasScope,\n    /** Observability: how many users currently have a refresh in flight. */\n    pendingRefreshCount: () => inFlight.size,\n  };\n}\n\n/**\n * Reference in-memory GrantStore. Real deployments swap this for a single\n * UPDATE ... SET access_token = $1, refresh_token = $2, expires_at = $3\n * statement — one row, one write, one transaction.\n */\nexport function createMemoryGrantStore(initial = {}) {\n  const rows = new Map(Object.entries(initial));\n  return {\n    async load(userId) {\n      const row = rows.get(userId);\n      return row ? { ...row } : null;\n    },\n    async save(userId, record) {\n      rows.set(userId, { ...record });\n    },\n    /** Test/debug affordance only. */\n    _rows: rows,\n  };\n}\n```\n\n## How to adapt it\n\n**Swap the store first.** `createMemoryGrantStore` exists so the tests can run; delete it in your copy. Implement `load` and `save` against your database, and make `save` one statement — a single `UPDATE` setting the access token, the refresh token and the expiry together. Two statements is the original bug with extra steps.\n\n**Decide how far the lock has to reach.** The in-flight `Map` serializes refreshes inside one process. That is enough for a single worker and not enough for a fleet: if several pods can refresh the same user, keep the shape and back the lock with a Postgres advisory lock or a Redis lock keyed on the user id, taken inside `performRefresh` and released in the same `finally`.\n\n**Point `expiryFromPayload` at your provider's field.** The function already handles `expires_at` in epoch seconds and `expires_in` in seconds, which covers the providers we have looked at, but confirm yours against its current docs rather than against this file.\n\n**Tune the buffer, do not remove it.** `refreshSkewMs` defaults to five minutes of remaining life. Refreshing on a buffer rather than on a 401 shrinks the window in which two workers can race at all. Note that at least one provider only mints a genuinely new access token when the current one is close to expiry, so verify the behaviour before tightening the number.\n\n**Wire `markRevoked` to your deauthorization signal.** Learning that a user disconnected from a failed refresh is the slow way. If your provider emits an event when someone revokes you, call `markRevoked` from that handler and stop the traffic before it starts.\n\n**Log `error_description`, not just the status.** `invalid_grant` is overloaded — the provider puts the real reason in the description field, and that string is the difference between a five-minute diagnosis and an afternoon.\n\n## The contract the tests hold it to\n\nEach bullet is one test in `cookbook/refresh-rotation.test.mjs`. They assert on observable state — call counts, what the store holds, which error class came back — never on the client's internal decisions.\n\n- Twenty-five concurrent callers for one expired user produce **exactly one** token-endpoint call, exactly one store write, and the same new token for everyone.\n- Two users refreshing at once produce **two** calls, not one: the lock is per user, not global.\n- Rotation persists the new refresh token, the new access token and the normalized expiry in **one** write, and that write is durable before the refresh promise resolves. The store commits on a later tick, so a missing `await` fails the test.\n- A token response with no `refresh_token` field leaves the stored one in place.\n- A 401 produces one refresh and one retry, in that order, with the retry carrying the new bearer token. A second 401 raises `UnauthorizedAfterRefreshError` instead of looping.\n- A 401 that the refresh fixes returns the retried response.\n- A 403 is returned to the caller untouched, with zero token-endpoint calls.\n- `invalid_grant` marks the grant dead, clears both tokens in the same write, and every later call — token or API — short-circuits with `DeadGrantError` and makes no network request at all.\n- A 503 from the token endpoint raises a retryable error, leaves the stored refresh token intact and active, releases the lock, and the very next attempt succeeds.\n- A token with plenty of life left is served from the store with no refresh; a token inside the proactive buffer is refreshed before it expires.\n- `markRevoked` kills a grant out of band, and `hasScope` reads the granted set — a declined scope reads as absent.\n- Scope parsing accepts both the space-delimited and the comma-delimited forms, because reusing one delimiter's parser on the other provider is a real and quiet bug.\n\n## What it deliberately does not do\n\nIt does not implement the authorization-code exchange, PKCE, or the initial consent flow — those are one-time paths with different failure modes, and the [integration guides](/integrate) cover them per provider. It does not rate-limit; a token refresh does not count against the API budget on at least one provider, but the calls you make with the token do. And it does not decide what to show a user whose grant just died. That is a product question, and the honest answer is usually a re-connect prompt rather than a silent retry."
  },
  {
    "slug": "webhook-receiver",
    "primaryQuery": "idempotent webhook receiver code example",
    "h1": "Recipe: A Replay-Safe Health Webhook Receiver",
    "metaTitle": "Recipe: A Replay-Safe Webhook Receiver",
    "metaDescription": "A tested, dependency-free ingest function: signature over raw bytes, delivery dedupe, thin pointer jobs, versioned replace, and a DLQ that still acks.",
    "updated": "2026-08-12",
    "answer": "A duplicated health webhook does not throw — it lands in an aggregate and quietly doubles somebody's day. This recipe is one ingest function that makes that impossible by construction: it verifies the HMAC over the raw request bytes before anything parses them, dedupes the delivery on its id, enqueues a thin pointer instead of trusting payload values, gates every effect on a monotonic version so an out-of-order delivery is a no-op, and routes a poisoned delivery to a dead-letter queue while still acknowledging. Every collaborator is injected, so the whole replay suite runs with no network and no tunnel. The behaviour is argued in full on the webhook ingestion page; this is the code.",
    "faqs": [
      {
        "q": "Why acknowledge a delivery that blew up inside the handler?",
        "a": "Because replaying the same bytes hits the same defect. The signature already proved who sent it, so answering with a 500 buys nothing except an escalating retry storm — and on platforms with an endpoint-disable policy, eventually an endpoint the provider stops talking to. The delivery goes to a dead-letter queue carrying the affected window, and recovery is a replay that re-pulls that window against a fixed adapter rather than re-applying a stored payload."
      },
      {
        "q": "Why does the enqueued job carry a window instead of the numbers from the payload?",
        "a": "Most fitness notifications are change pointers rather than data carriers, so the values in the body are either absent or untrustworthy. Keying the job on user, provider, metric and window makes the effect idempotent by construction: re-fetching a window and replacing what you hold is naturally repeatable, while parsing a delta out of a ping and applying it never will be. It also collapses eleven pings about the same Tuesday into one provider call."
      },
      {
        "q": "Which parts of this file will I have to rewrite for my provider?",
        "a": "Three, and they are isolated for exactly that reason. The hmac function, because signature schemes differ per vendor and some are not publicly documented. The header names, because the id, timestamp and signature headers are vendor-specific. And parsePointer, because only you know how your provider names its subject id, metric and window. Write the fixture from a real captured delivery rather than from a spec example, and the rest of the file stays as it is."
      },
      {
        "q": "What stops a late delivery from resurrecting a user who asked to be erased?",
        "a": "Subject resolution happens before any store is touched, and an unresolvable subject is acknowledged and dropped with no row written anywhere — not a delivery row, not a dead-letter row. The dead-letter queue is a store like any other and it is the one teams forget. The resolver must also be driven by a tombstone rather than by a missing user row, or an ingest path that reads absence as create-one will pass against an empty test database and resurrect a real erased person in production."
      }
    ],
    "related": [
      {
        "href": "/architecture/webhook-ingestion",
        "label": "Designing webhook ingestion for health data"
      },
      {
        "href": "/test/webhooks-locally",
        "label": "Testing webhooks locally"
      },
      {
        "href": "/learn/what-are-webhooks",
        "label": "What webhooks are, and why fitness APIs use them"
      },
      {
        "href": "/fix/strava-webhook-not-firing",
        "label": "Fix: Strava webhook not firing"
      }
    ],
    "cta": {
      "pitch": "The duplicate that doubles somebody's step count never announces itself. Subscribe for the next recipe that makes a silent failure loud."
    },
    "body": "A duplicated health webhook does not throw. It lands in an aggregate, and a doubled calorie total just looks like a good Tuesday — until a support ticket arrives months later and you spend an afternoon proving it was your ingest path rather than the user's watch.\n\n## The shape, in one paragraph\n\n[Designing webhook ingestion for health data](/architecture/webhook-ingestion) argues the whole design; this page is the code that falls out of it. The load-bearing decisions are that the delivery and the effect need **separate** idempotency layers, that the effect must be a replace rather than an increment, and that the endpoint must answer in milliseconds because a slow handler manufactures the very retries it is defending against. The event itself is a thin pointer — most fitness notifications tell you that something about a user, in a window, changed, and nothing more — which is exactly what makes the effect idempotent by construction: re-fetching a window and replacing what you hold is naturally repeatable, and parsing a delta out of a ping never will be. If webhooks themselves are new to you, start at [what webhooks are](/learn/what-are-webhooks).\n\nThe one number the endpoint must compute carefully is the day. A civil date stored at ingest and a UTC day derived at read time are not the same thing, and the difference is a daily total that is nine hours wrong for every user in a positive offset — see [whose midnight defines the day](/architecture/timezones-and-day-boundaries).\n\n## The implementation\n\nThe canonical file is `cookbook/webhook-receiver.mjs` in this site's repository, and `cookbook/webhook-receiver.test.mjs` runs against it on every CI run. The listing below is a verbatim copy. It has no runtime dependencies beyond `node:crypto`, needs Node 20 or newer, and takes its secrets, stores, queue, dead-letter queue, clock and subject resolver as arguments — so the entire replay suite runs with no network and no tunnel.\n\n```js\n/**\n * webhook-receiver.mjs — a health-webhook ingest endpoint that is safe to\n * deliver to twice.\n *\n * WHAT THIS IMPLEMENTS\n *   `ingest(rawBody, headers, deps)` — everything a fitness-provider webhook\n *   endpoint should do before it answers, and nothing it should not:\n *     1. Verify the HMAC signature over the RAW request bytes, BEFORE parsing.\n *        Parsing JSON and re-serializing it breaks signatures over whitespace.\n *        The signature header is a list, so key rotation works.\n *     2. Reject a signed timestamp outside the replay tolerance.\n *     3. Resolve the subject to one of your users. An unresolvable subject is\n *        acknowledged and dropped with NO row written anywhere — including the\n *        DLQ — because an erased user must not be resurrected by a late event.\n *     4. Dedupe the DELIVERY on (provider, delivery id).\n *     5. Treat the event as a thin POINTER. The job carries\n *        (user, provider, metric, window, version) and never a value read out\n *        of the payload; the worker re-fetches the window from the provider.\n *     6. Gate on a monotonic version so an out-of-order delivery is a no-op\n *        instead of a regression. Never resolve on arrival time.\n *     7. Route a handler exception to a DLQ carrying the window, and still ACK.\n *   The endpoint does no provider I/O: acknowledging in milliseconds is what\n *   stops you from manufacturing the retries you are defending against.\n *\n * PATTERN DOCUMENTED AT\n *   https://aifitnessapi.com/architecture/webhook-ingestion\n *   https://aifitnessapi.com/test/webhooks-locally\n *\n * Signature scheme follows the Standard Webhooks shape\n * (`msg_id.timestamp.payload`, a space-delimited `v1,<sig>` list). Real\n * providers differ — swap `hmac`, `headerNames` and `parsePointer` per provider\n * and write your fixtures from a captured delivery, not from a spec example.\n *\n * MIT — from aifitnessapi.com/cookbook\n */\nimport { createHmac, timingSafeEqual } from \"node:crypto\";\n\nexport const DEFAULT_HEADER_NAMES = {\n  id: \"webhook-id\",\n  timestamp: \"webhook-timestamp\",\n  signature: \"webhook-signature\",\n};\n\n/** Standard Webhooks defines no replay window. Pick one; make it a parameter. */\nconst DEFAULT_TOLERANCE_MS = 5 * 60 * 1000;\n\nfunction toBytes(raw) {\n  if (Buffer.isBuffer(raw)) return raw;\n  if (raw instanceof Uint8Array) return Buffer.from(raw.buffer, raw.byteOffset, raw.byteLength);\n  if (typeof raw === \"string\") return Buffer.from(raw, \"utf8\");\n  throw new TypeError(\"rawBody must be Buffer, Uint8Array, or string\");\n}\n\n/** Works with a plain object, a Map, or a WHATWG Headers instance. */\nfunction headerReader(headers) {\n  if (headers && typeof headers.get === \"function\") return (name) => headers.get(name);\n  const lower = new Map(\n    Object.entries(headers ?? {}).map(([k, v]) => [k.toLowerCase(), Array.isArray(v) ? v[0] : v]),\n  );\n  return (name) => lower.get(name.toLowerCase()) ?? null;\n}\n\n/**\n * Default HMAC-SHA256, base64 out. Standard Webhooks secrets look like\n * `whsec_<base64>` and the KEY is the decoded base64, not the literal string.\n */\nexport function defaultHmacSha256(secret, messageBytes) {\n  const material = String(secret).startsWith(\"whsec_\") ? String(secret).slice(6) : String(secret);\n  let key;\n  try {\n    key = Buffer.from(material, \"base64\");\n    if (key.length === 0) key = Buffer.from(material, \"utf8\");\n  } catch {\n    key = Buffer.from(material, \"utf8\");\n  }\n  return createHmac(\"sha256\", key).update(messageBytes).digest(\"base64\");\n}\n\nfunction constantTimeEquals(a, b) {\n  const ba = Buffer.from(String(a));\n  const bb = Buffer.from(String(b));\n  if (ba.length !== bb.length) return false;\n  return timingSafeEqual(ba, bb);\n}\n\n/**\n * Verify a signature over the exact bytes received.\n *\n * The header carries a space-delimited LIST so a producer can sign with the\n * old and the new secret during a rotation. Accepting only the first entry\n * passes the happy path for months and fails on rotation morning.\n */\nexport function verifySignature({ rawBody, id, timestamp, signatureHeader, secrets, hmac = defaultHmacSha256 }) {\n  if (!signatureHeader || !Array.isArray(secrets) || secrets.length === 0) return false;\n  const signed = Buffer.concat([Buffer.from(`${id}.${timestamp}.`, \"utf8\"), toBytes(rawBody)]);\n  const presented = String(signatureHeader)\n    .split(/\\s+/)\n    .filter(Boolean)\n    .map((part) => (part.includes(\",\") ? part.slice(part.indexOf(\",\") + 1) : part));\n\n  let matched = false;\n  for (const secret of secrets) {\n    const expected = hmac(secret, signed);\n    for (const candidate of presented) {\n      // No early return: keep the work constant across secrets and signatures.\n      if (constantTimeEquals(expected, candidate)) matched = true;\n    }\n  }\n  return matched;\n}\n\n/**\n * Turn a decoded event into the pointer the fetch worker needs. This is the\n * one function you rewrite per provider — the envelope below is a neutral\n * shape, not any real vendor's.\n */\nexport function defaultParsePointer(event) {\n  const subjectId = event?.subject_id ?? event?.ownerId ?? event?.owner_id ?? event?.userId;\n  if (subjectId === undefined || subjectId === null || subjectId === \"\") {\n    throw new Error(\"event carries no subject id\");\n  }\n  const metric = event?.metric ?? event?.collectionType ?? \"unknown\";\n  const windowStart = event?.window_start ?? event?.date ?? null;\n  if (!windowStart) throw new Error(\"event carries no window\");\n  const version = Number(event?.version ?? event?.modified_at ?? 0);\n  if (!Number.isFinite(version)) throw new Error(\"event carries an unusable version\");\n  return {\n    subjectId: String(subjectId),\n    metric: String(metric),\n    windowStart: String(windowStart),\n    windowEnd: event?.window_end ? String(event.window_end) : String(windowStart),\n    version,\n  };\n}\n\n/** The debounce key. Eleven pings about the same Tuesday collapse into one job. */\nexport function jobKeyFor({ provider, userId, metric, windowStart }) {\n  return `${provider}:${userId}:${metric}:${windowStart}`;\n}\n\nconst REJECT = (outcome, detail) => ({ status: 400, ack: false, outcome, detail });\nconst ACK = (outcome, extra = {}) => ({ status: 200, ack: true, outcome, ...extra });\n\n/**\n * @param {Buffer|Uint8Array|string} rawBody  EXACTLY the bytes the provider sent.\n * @param {object|Headers} headers\n * @param {object} deps\n * @param {string[]} deps.secrets            all currently-valid secrets (rotation)\n * @param {(subjectId: string) => Promise<string|null>} deps.resolveSubject\n *        Must be driven by a tombstone, not by \"no user row\" — an ingest path\n *        that reads a missing user as \"create one\" passes against an empty test\n *        database and resurrects an erased person against a real one.\n * @param {{insertIfAbsent: (key: string, row: object) => Promise<boolean>}} deps.seen\n * @param {{commitVersion: (key: string, version: number) => Promise<boolean>}} deps.pointers\n * @param {{enqueue: (job: object) => Promise<void>}} deps.queue\n * @param {{add: (entry: object) => Promise<void>}} deps.dlq\n * @param {string} [deps.provider]\n * @param {(secret: string, bytes: Buffer) => string} [deps.hmac]\n * @param {(bytes: Buffer) => object} [deps.parseBody]\n * @param {(event: object, headers: object) => object} [deps.parsePointer]\n * @param {() => number} [deps.now]\n * @param {number} [deps.toleranceMs]\n * @param {object} [deps.headerNames]\n */\nexport async function ingest(rawBody, headers, deps) {\n  const {\n    secrets,\n    resolveSubject,\n    seen,\n    pointers,\n    queue,\n    dlq,\n    provider = \"unknown\",\n    hmac = defaultHmacSha256,\n    parseBody = (bytes) => JSON.parse(bytes.toString(\"utf8\")),\n    parsePointer = defaultParsePointer,\n    now = () => Date.now(),\n    toleranceMs = DEFAULT_TOLERANCE_MS,\n    headerNames = DEFAULT_HEADER_NAMES,\n  } = deps ?? {};\n\n  const bytes = toBytes(rawBody);\n  const read = headerReader(headers);\n  const deliveryId = read(headerNames.id);\n  const timestamp = read(headerNames.timestamp);\n  const signatureHeader = read(headerNames.signature);\n\n  if (!deliveryId || !timestamp) return REJECT(\"missing-headers\");\n\n  // ---- 1. Signature, over the raw bytes, before anything parses them. ------\n  if (!verifySignature({ rawBody: bytes, id: deliveryId, timestamp, signatureHeader, secrets, hmac })) {\n    return REJECT(\"invalid-signature\");\n  }\n\n  // ---- 2. Replay tolerance. A correctly signed delivery from last week is\n  //         still a replay. Checked before dedupe so it costs no storage. ----\n  const signedAtMs = Number(timestamp) * 1000;\n  if (!Number.isFinite(signedAtMs) || Math.abs(now() - signedAtMs) > toleranceMs) {\n    return REJECT(\"stale-timestamp\");\n  }\n\n  let pointer = null;\n  let userId = null;\n  try {\n    // ---- 3. Parse, then resolve the subject. ------------------------------\n    const event = parseBody(bytes);\n    pointer = parsePointer(event, headers);\n\n    userId = await resolveSubject(pointer.subjectId);\n    if (!userId) {\n      // Acknowledge so the provider stops retrying, and write NOTHING — not a\n      // delivery row, not a DLQ row. Then go revoke the subscription: the row\n      // is not the problem, the live subscription is.\n      return ACK(\"unresolved-subject\", { revokeSubscription: true, subjectId: pointer.subjectId });\n    }\n\n    // ---- 4. Delivery-level dedupe. A duplicate is a SUCCESS: the provider\n    //         delivered and we acknowledged. Answering non-2xx here converts\n    //         one duplicate into an escalating retry storm. -----------------\n    const deliveryKey = `${provider}:${deliveryId}`;\n    const fresh = await seen.insertIfAbsent(deliveryKey, {\n      provider,\n      deliveryId,\n      userId,\n      metric: pointer.metric,\n      windowStart: pointer.windowStart,\n      signedAtMs,\n      receivedAtMs: now(),\n    });\n    if (!fresh) return ACK(\"duplicate\", { deliveryId });\n\n    // ---- 5/6. Versioned gate on the pointer. A backlog flush delivers\n    //           two-day-old events after fresh ones; ordering on arrival would\n    //           overwrite a correct day with a stale one, silently. ---------\n    const key = jobKeyFor({ provider, userId, metric: pointer.metric, windowStart: pointer.windowStart });\n    const advanced = await pointers.commitVersion(key, pointer.version);\n    if (!advanced) return ACK(\"stale-version\", { deliveryId, key, version: pointer.version });\n\n    // ---- 7. Enqueue a thin pointer. No value from the payload travels with\n    //         it; the worker re-reads the window and does a versioned replace,\n    //         which is idempotent by construction. ------------------------\n    const job = {\n      key,\n      provider,\n      userId,\n      metric: pointer.metric,\n      windowStart: pointer.windowStart,\n      windowEnd: pointer.windowEnd,\n      version: pointer.version,\n      deliveryId,\n    };\n    await queue.enqueue(job);\n    return ACK(\"enqueued\", { deliveryId, job });\n  } catch (error) {\n    // ACK DECISION, DELIBERATE: a poisoned delivery is acknowledged (200), not\n    // 500'd. The signature already proved the sender; replaying the same bytes\n    // will hit the same defect, so a non-2xx only buys an escalating retry\n    // storm and, on platforms with a disable policy, an endpoint the provider\n    // eventually stops talking to. Recovery is a DLQ replay that RE-PULLS the\n    // window against a fixed adapter — which is why the window, not just the\n    // body, is what we store.\n    await dlq.add({\n      provider,\n      deliveryId,\n      userId,\n      metric: pointer?.metric ?? null,\n      windowStart: pointer?.windowStart ?? null,\n      windowEnd: pointer?.windowEnd ?? null,\n      rawBody: bytes,\n      error: String(error?.message ?? error),\n      failedAtMs: now(),\n    });\n    return ACK(\"dead-lettered\", { deliveryId, error: String(error?.message ?? error) });\n  }\n}\n\n/**\n * The write the fetch worker performs after re-reading the window. REPLACE,\n * never increment, gated on a monotonic version — the SQL equivalent is\n * `on conflict ... do update set value = excluded.value\n *  where excluded.version > daily_rollup.version`.\n *\n * @returns {Promise<boolean>} true when the row moved.\n */\nexport async function applyVersionedReplace(store, key, { value, version, ...rest }) {\n  const current = await store.read(key);\n  if (current && Number(current.version) >= Number(version)) return false;\n  await store.write(key, { ...rest, value, version: Number(version) });\n  return true;\n}\n\n/**\n * Reference seen-store. In Postgres this is\n * `insert into webhook_delivery ... on conflict do nothing` and the boolean is\n * the row count. Rows must live at least as long as the provider's retry\n * horizon — and must be purged with the user on erasure, or a late delivery\n * re-materializes somebody you deleted.\n */\nexport function createMemorySeenStore() {\n  const rows = new Map();\n  return {\n    async insertIfAbsent(key, row) {\n      if (rows.has(key)) return false;\n      rows.set(key, row);\n      return true;\n    },\n    get size() {\n      return rows.size;\n    },\n    rows,\n  };\n}\n\n/** Reference version gate. In Postgres: an upsert with a `where excluded.version > ...`. */\nexport function createMemoryVersionGate() {\n  const versions = new Map();\n  return {\n    async commitVersion(key, version) {\n      const current = versions.get(key);\n      if (current !== undefined && current >= version) return false;\n      versions.set(key, version);\n      return true;\n    },\n    async readVersion(key) {\n      return versions.get(key);\n    },\n    versions,\n  };\n}\n```\n\n## How to adapt it\n\n**Three functions are provider-specific, and they are separated for that reason.** `defaultHmacSha256` implements one scheme; yours may differ, and some vendors' schemes are not publicly documented, so read the provider's docs and write your fixture from a captured delivery rather than from a spec example. `DEFAULT_HEADER_NAMES` carries the id, timestamp and signature header names, which vary. `defaultParsePointer` reads a neutral envelope that is nobody's real payload — replace it with one parser per provider, and keep it throwing on a missing subject or window, because a pointer you cannot build is a delivery that belongs in the dead-letter queue.\n\n**Make `resolveSubject` tombstone-driven.** It must return null for a user who was erased, not merely for a user who is absent. An ingest path that reads \"no such user\" as \"create one\" passes happily against an empty test database and resurrects a real erased person in production.\n\n**Replace the two reference stores.** `createMemorySeenStore` is an `INSERT ... ON CONFLICT DO NOTHING` and the boolean is the affected row count. `createMemoryVersionGate` is an upsert whose update clause is gated on the incoming version being the larger one. Both belong in your database, not in process memory, and both need a retention policy: the delivery rows must outlive the provider's maximum retry horizon, and they must be purged in the same job that erases the user, or a late delivery re-materializes somebody you deleted.\n\n**Pick a replay tolerance and make it a parameter.** The five-minute default here is a choice, not a specification. Put yours in config and make the boundary a test parameter rather than a constant buried in the verifier.\n\n**Keep the signature loop, even before you rotate.** The header is a list precisely so a producer can sign with the old and the new secret at once. A verifier written against a single signature passes for months and fails on the morning you rotate.\n\n**Run a reconciliation sweep anyway.** At-least-once delivery is a promise about events that get sent. It says nothing about the events that were never sent because your endpoint was failing during a deploy. This file makes each delivery safe; it cannot make a missing delivery appear.\n\n## The contract the tests hold it to\n\nEach bullet is one test in `cookbook/webhook-receiver.test.mjs`. Every assertion is about enqueued jobs and stored state, never about a status code on its own — a duplicate delivery answers 200 too, so the status code cannot tell you whether the second one did nothing or did everything twice.\n\n- A bad signature is rejected with a 400 and no acknowledgement, and the injected body parser is **never called**: the bytes are rejected before anything reads them. Nothing is written to the queue, the dedupe table, or the dead-letter queue.\n- Verification is over the raw bytes: a body that is parsed and re-serialized with one stray byte no longer verifies, which is the whole reason to hold the original buffer.\n- During a rotation, a delivery signed with both the old and the new secret is accepted; once the old secret is dropped from the configured list, a delivery signed only with it is rejected.\n- A correctly signed delivery from three days ago is rejected on the timestamp tolerance, before it costs a dedupe row.\n- A byte-identical replay of the same delivery id is acknowledged with a 200 and enqueues **nothing** — one job, not two.\n- The enqueued job is a thin pointer: user, provider, metric, window, version, delivery id. The payload's own value field does not appear on it.\n- An older version arriving after a newer one is acknowledged, enqueues nothing, and leaves the stored version where it was. The backlog flush cannot regress the day.\n- A genuine re-notification for the same day with a higher version **does** enqueue again, because a day that fills in is a real update rather than a duplicate.\n- An unresolvable subject is acknowledged and dropped with no row written anywhere, including the dead-letter queue, and the result asks the caller to revoke the subscription rather than fix a row.\n- A handler exception dead-letters an entry carrying the user, the metric, the window and the verified bytes, and still returns 200. The reasoning for that acknowledgement is written into the `catch` block itself.\n- A malformed body with a valid signature is dead-lettered with a null window rather than a partial write.\n- `applyVersionedReplace` replaces rather than increments, ignores a stale version, and treats an equal version as a no-op — so 2,100 steps followed by 8,431 steps is 8,431, and never 10,531.\n\n## What it deliberately does not do\n\nIt does not talk to the provider. That belongs in the fetch worker, behind its own rate-limit handling, precisely so a provider's bad day cannot slow your endpoint into generating more retries. It does not implement the subscription handshake, which is a one-time setup event with the loudest failure mode in the whole area — nothing arrives at all — and is covered in [testing webhooks locally](/test/webhooks-locally). And it cannot recover a deletion it never heard about: whether you can act on a provider's deletion signal is a schema decision made months before the first one arrives."
  },
  {
    "slug": "rate-limit-fetcher",
    "primaryQuery": "retry after 429 exponential backoff fetch wrapper",
    "h1": "Recipe: A Rate-Limit-Aware Fetch Wrapper",
    "metaTitle": "Recipe: A Rate-Limit-Aware Fetch Wrapper",
    "metaDescription": "A tested, dependency-free fetch wrapper: per-user budgets, Retry-After in both forms, full-jitter backoff, a breaker that degrades, and no unsafe retries.",
    "updated": "2026-08-12",
    "answer": "Fitness providers meter reads per consented user, so one runaway backfill starves that user and adding workers makes it worse. This recipe is a fetch wrapper that tracks a per-user budget, honours Retry-After in both the delay-seconds and HTTP-date forms, backs off 5xx with full jitter so a recovering provider does not get a synchronized stampede, opens a circuit that skips and records a gap rather than hammering a dead endpoint, and never replays a non-idempotent call it cannot prove failed. The clock, sleep, jitter source and fetch are injected, so the whole fault-injection suite runs against a fake clock in milliseconds. Nothing throws on an HTTP outcome: every call returns an envelope so the caller parks the window and moves on.",
    "faqs": [
      {
        "q": "Why does a 429 not count towards opening the circuit?",
        "a": "A provider answering 429 is healthy and telling you the truth about your allowance. Tripping a breaker on it would degrade a service that is working correctly, and it would keep degrading for as long as you are over quota. In this wrapper only 5xx responses and transport errors increment the failure counter; a 429 consumes the retry budget instead, and once that budget is spent the call returns a rate-limited envelope with the time to retry so the caller can park the window."
      },
      {
        "q": "What makes full jitter better than a fixed backoff here?",
        "a": "A fixed backoff passes every single-user test you will ever write and then synchronizes your whole fleet onto the same second the moment a provider recovers. Full jitter draws the delay uniformly from zero up to the exponential ceiling, which spreads recovery arrivals across the window instead of stacking them. That failure only shows up in a test with more than one user in the fixture, which is why the contract below asserts on the distribution rather than on a single sleep."
      },
      {
        "q": "Why does the wrapper return an envelope instead of throwing on failure?",
        "a": "Because a backfill worker handling hundreds of windows should park the ones it could not read and carry on, not die on the first outage. Every call resolves with an outcome, the attempt count, how long it waited, and when it is worth trying again. Skipped and rate-limited outcomes also push an entry onto a gaps list, which is the input to the reconciliation sweep that eventually re-reads what you missed."
      },
      {
        "q": "How does the wrapper decide a call is too risky to replay?",
        "a": "It looks at the method, and you can override it per call. GET, HEAD, OPTIONS, PUT and DELETE are treated as safe to repeat; POST and PATCH are not. When a call in the unsafe set dies in transit or comes back 5xx, the outcome is marked ambiguous and returned immediately with no retry, because the server may have applied the write before failing. A provider that documents a specific POST as idempotent can opt that one call back in."
      }
    ],
    "related": [
      {
        "href": "/fix/fitbit-api-429-rate-limit",
        "label": "Fix: Fitbit API 429 rate limit"
      },
      {
        "href": "/test/rate-limits-and-outages",
        "label": "Testing rate limits and outages"
      },
      {
        "href": "/architecture/historical-backfill",
        "label": "Designing a historical backfill"
      },
      {
        "href": "/architecture/caching-fitness-api-responses",
        "label": "Caching fitness API responses"
      }
    ],
    "cta": {
      "pitch": "Quota maths, retry policy and the parts of a provider's behaviour nobody documents — we take one apart every week. Subscribe for the next teardown."
    },
    "body": "Day 400 of a three-year backfill, the provider starts answering `429`, your worker retries into the wall, the pod hits its deadline, and the next attempt starts the user's history again from the beginning. That is the expensive failure. The cheap one is a response that lands just inside your timeout: nothing errors, nothing alerts, and a backfill budgeted for twenty minutes is still running tomorrow.\n\n## Why a per-user budget changes the shape\n\nThe single fact that makes fitness rate limits different is that at least one major provider meters reads **per consented user** rather than per app — the ranked causes and the header names are in [the Fitbit 429 fix](/fix/fitbit-api-429-rate-limit), which carries the published figure with its verify caveat attached. Two consequences follow. One runaway loop starves exactly one user and touches nobody else's quota, so the fix is usually local to how you call one person's endpoints. And you cannot buy your way out by adding workers, because more workers on the same user is more pressure on the same bucket.\n\nSo the budget in this wrapper is keyed by user, and it re-syncs from the provider's own rate-limit headers rather than trusting the number you configured. Do not build anything around a specific requests-per-hour figure: make the ceiling a parameter, read the allowance at runtime where the provider reports it, and assert on degradation rather than on throughput. [Testing rate limits and outages](/test/rate-limits-and-outages) makes the case for that at length, including why a unit test of your backoff helper cannot fail.\n\n## The implementation\n\nThe canonical file is `cookbook/rate-limit-fetcher.mjs` in this site's repository, and `cookbook/rate-limit-fetcher.test.mjs` runs against it on every CI run. The listing below is a verbatim copy. It has no runtime dependencies, needs Node 20 or newer, and injects `fetch`, the clock, `sleep` and the jitter source — so the whole fault-injection suite runs against a fake clock in milliseconds of real time.\n\n```js\n/**\n * rate-limit-fetcher.mjs — a fetch wrapper that survives a per-user quota.\n *\n * WHAT THIS IMPLEMENTS\n *   Fitness providers meter reads PER CONSENTED USER, not per app, so one\n *   runaway backfill starves that user and nobody else — and adding workers\n *   makes it worse, not better. This wrapper:\n *     1. Tracks a per-user request budget in a rolling window, and re-syncs it\n *        from the provider's own rate-limit headers rather than trusting a\n *        hard-coded number.\n *     2. Honours `Retry-After` on 429 in BOTH forms RFC 9110 §10.2.3 permits:\n *        delay-seconds and HTTP-date. RFC 6585 §4 only says a 429 MAY carry\n *        the header, so a missing header falls back to jittered backoff.\n *     3. Backs off 5xx with exponential backoff and FULL JITTER, so every\n *        worker does not resynchronize onto the same second when a provider\n *        recovers.\n *     4. Opens a circuit after N consecutive failures per origin and then\n *        DEGRADES — skips the call and records the gap for a later sweep —\n *        instead of hammering a provider that is already down.\n *     5. Never retries a non-idempotent call on an ambiguous failure. A POST\n *        that died in transit, or answered 5xx, may or may not have landed.\n *   Nothing throws for an HTTP outcome: every call returns an envelope, so a\n *   caller parks the window and moves on rather than crashing a worker.\n *\n * PATTERN DOCUMENTED AT\n *   https://aifitnessapi.com/fix/fitbit-api-429-rate-limit\n *   https://aifitnessapi.com/test/rate-limits-and-outages\n *\n * Clock, sleep, jitter source and fetch are all injected, so the tests below\n * run in microseconds against a fake clock and never touch a network.\n *\n * MIT — from aifitnessapi.com/cookbook\n */\n\n/** Methods safe to replay when we cannot tell whether the call landed. */\nexport const IDEMPOTENT_METHODS = new Set([\"GET\", \"HEAD\", \"OPTIONS\", \"PUT\", \"DELETE\"]);\n\n/** Headers providers use to report the remaining allowance, in priority order. */\nconst REMAINING_HEADERS = [\n  \"fitbit-rate-limit-remaining\",\n  \"x-ratelimit-remaining\",\n  \"ratelimit-remaining\",\n];\n/** Headers reporting seconds until the window resets. */\nconst RESET_HEADERS = [\"fitbit-rate-limit-reset\", \"x-ratelimit-reset\", \"ratelimit-reset\"];\n\nexport function headerOf(res, name) {\n  const h = res?.headers;\n  if (!h) return null;\n  if (typeof h.get === \"function\") return h.get(name);\n  const lower = name.toLowerCase();\n  for (const [k, v] of Object.entries(h)) {\n    if (k.toLowerCase() === lower) return Array.isArray(v) ? v[0] : v;\n  }\n  return null;\n}\n\n/**\n * Parse `Retry-After` into milliseconds. RFC 9110 §10.2.3 allows delay-seconds\n * OR an HTTP-date; a parser that only calls parseInt is non-compliant and will\n * silently fall through to a default on any provider that sends the date form.\n *\n * @returns {number|null} ms to wait, clamped at 0, or null if unparseable.\n */\nexport function parseRetryAfter(value, nowMs) {\n  if (value === null || value === undefined || value === \"\") return null;\n  const raw = String(value).trim();\n  if (/^\\d+$/.test(raw)) return Number(raw) * 1000;\n  const at = Date.parse(raw);\n  if (Number.isNaN(at)) return null;\n  return Math.max(0, at - nowMs);\n}\n\n/**\n * Exponential backoff with FULL jitter: uniform in [0, min(cap, base * 2^n)].\n * Fixed backoff passes every single-user test and then synchronizes your whole\n * fleet onto one second the moment the provider comes back.\n */\nexport function fullJitterDelay(attempt, { baseDelayMs = 500, maxDelayMs = 60_000, random = Math.random } = {}) {\n  const exponential = Math.min(maxDelayMs, baseDelayMs * 2 ** Math.max(0, attempt - 1));\n  return Math.floor(random() * exponential);\n}\n\n/** Per-user rolling request budget. Reset is a wall-clock window, not a tick. */\nfunction createBudgetTracker({ limit, windowMs, now }) {\n  const users = new Map();\n  function stateFor(userId) {\n    let s = users.get(userId);\n    if (!s || now() >= s.resetAt) {\n      s = { remaining: limit, resetAt: now() + windowMs, limit };\n      users.set(userId, s);\n    }\n    return s;\n  }\n  return {\n    take(userId) {\n      const s = stateFor(userId);\n      if (s.remaining <= 0) return { ok: false, resetAt: s.resetAt };\n      s.remaining -= 1;\n      return { ok: true, resetAt: s.resetAt };\n    },\n    /** Trust the provider's count over ours when it tells us one. */\n    syncFromResponse(userId, res) {\n      const s = stateFor(userId);\n      for (const name of REMAINING_HEADERS) {\n        const v = headerOf(res, name);\n        if (v !== null && v !== undefined && v !== \"\" && Number.isFinite(Number(v))) {\n          s.remaining = Number(v);\n          break;\n        }\n      }\n      for (const name of RESET_HEADERS) {\n        const v = headerOf(res, name);\n        if (v !== null && v !== undefined && v !== \"\" && Number.isFinite(Number(v))) {\n          s.resetAt = now() + Number(v) * 1000;\n          break;\n        }\n      }\n    },\n    peek: (userId) => ({ ...stateFor(userId) }),\n  };\n}\n\n/**\n * Per-origin circuit breaker with a half-open probe.\n * 429 deliberately does NOT count as a failure: a provider answering 429 is\n * healthy and telling you the truth. Only 5xx and transport errors trip it.\n */\nfunction createBreaker({ threshold, cooldownMs, now }) {\n  const keys = new Map();\n  const stateFor = (key) => {\n    let s = keys.get(key);\n    if (!s) {\n      s = { failures: 0, openUntil: 0, halfOpen: false };\n      keys.set(key, s);\n    }\n    return s;\n  };\n  return {\n    /** @returns {{blocked: boolean, retryAtMs?: number}} */\n    check(key) {\n      const s = stateFor(key);\n      if (s.openUntil === 0) return { blocked: false };\n      if (now() < s.openUntil) return { blocked: true, retryAtMs: s.openUntil };\n      s.halfOpen = true; // one probe allowed through\n      return { blocked: false };\n    },\n    recordSuccess(key) {\n      const s = stateFor(key);\n      s.failures = 0;\n      s.openUntil = 0;\n      s.halfOpen = false;\n    },\n    recordFailure(key) {\n      const s = stateFor(key);\n      s.failures += 1;\n      if (s.halfOpen || s.failures >= threshold) {\n        s.openUntil = now() + cooldownMs;\n        s.halfOpen = false;\n      }\n    },\n    peek: (key) => ({ ...stateFor(key) }),\n  };\n}\n\n/**\n * @param {object} options\n * @param {typeof globalThis.fetch} [options.fetch]\n * @param {() => number} [options.now]              epoch ms\n * @param {(ms: number) => Promise<void>} [options.sleep]\n * @param {() => number} [options.random]           jitter source\n * @param {{limit: number, windowMs: number}} [options.budget]\n * @param {{threshold: number, cooldownMs: number}} [options.breaker]\n * @param {number} [options.maxAttempts]\n * @param {number} [options.baseDelayMs]\n * @param {number} [options.maxDelayMs]\n * @param {number} [options.maxWaitMs]  refuse to sit on a Retry-After longer\n *                                      than this; park the window instead\n * @param {(gap: object) => void} [options.onGap]\n * @param {(url: string) => string} [options.scopeOf]  breaker key\n */\nexport function createRateLimitedFetcher({\n  fetch: fetchImpl = globalThis.fetch,\n  now = () => Date.now(),\n  sleep = (ms) => new Promise((resolve) => setTimeout(resolve, ms)),\n  random = Math.random,\n  budget: budgetOpts = { limit: 150, windowMs: 3_600_000 },\n  breaker: breakerOpts = { threshold: 5, cooldownMs: 60_000 },\n  maxAttempts = 4,\n  baseDelayMs = 500,\n  maxDelayMs = 60_000,\n  maxWaitMs = 15 * 60_000,\n  onGap = () => {},\n  scopeOf = (url) => {\n    try {\n      return new URL(String(url)).origin;\n    } catch {\n      return String(url);\n    }\n  },\n} = {}) {\n  if (typeof fetchImpl !== \"function\") throw new TypeError(\"fetch is required\");\n\n  const budget = createBudgetTracker({ ...budgetOpts, now });\n  const breaker = createBreaker({ ...breakerOpts, now });\n  /** Every window we did not read. A reconciliation sweep drains this. */\n  const gaps = [];\n\n  function recordGap(entry) {\n    const gap = { at: now(), ...entry };\n    gaps.push(gap);\n    onGap(gap);\n    return gap;\n  }\n\n  /**\n   * @returns {Promise<{\n   *   outcome: \"ok\"|\"rate-limited\"|\"failed\"|\"ambiguous\"|\"skipped\",\n   *   response?: object, error?: Error, status: number|null,\n   *   attempts: number, waitedMs: number, reason?: string,\n   *   retryAtMs?: number, ambiguous?: boolean\n   * }>}\n   */\n  async function fetchForUser(userId, url, init = {}) {\n    const key = scopeOf(url);\n    const method = String(init.method ?? \"GET\").toUpperCase();\n    const idempotent = init.idempotent ?? IDEMPOTENT_METHODS.has(method);\n    let attempts = 0;\n    let waitedMs = 0;\n\n    for (;;) {\n      // --- Circuit first: a degrade is cheaper than a doomed request. -------\n      const circuit = breaker.check(key);\n      if (circuit.blocked) {\n        recordGap({ userId, url: String(url), reason: \"circuit-open\", retryAtMs: circuit.retryAtMs });\n        return {\n          outcome: \"skipped\",\n          reason: \"circuit-open\",\n          status: null,\n          attempts,\n          waitedMs,\n          retryAtMs: circuit.retryAtMs,\n        };\n      }\n\n      // --- Then the per-user budget. One user's exhaustion is one user's. ---\n      const allowance = budget.take(userId);\n      if (!allowance.ok) {\n        recordGap({ userId, url: String(url), reason: \"budget-exhausted\", retryAtMs: allowance.resetAt });\n        return {\n          outcome: \"skipped\",\n          reason: \"budget-exhausted\",\n          status: null,\n          attempts,\n          waitedMs,\n          retryAtMs: allowance.resetAt,\n        };\n      }\n\n      attempts += 1;\n      let res;\n      try {\n        res = await fetchImpl(url, init);\n      } catch (error) {\n        // Transport died. We do not know whether the server saw the request.\n        breaker.recordFailure(key);\n        if (!idempotent) {\n          recordGap({ userId, url: String(url), reason: \"ambiguous-transport-error\", method });\n          return { outcome: \"ambiguous\", ambiguous: true, error, status: null, attempts, waitedMs, reason: \"non-idempotent\" };\n        }\n        if (attempts >= maxAttempts) {\n          recordGap({ userId, url: String(url), reason: \"transport-error\" });\n          return { outcome: \"failed\", error, status: null, attempts, waitedMs, reason: \"transport-error\" };\n        }\n        const delay = fullJitterDelay(attempts, { baseDelayMs, maxDelayMs, random });\n        await sleep(delay);\n        waitedMs += delay;\n        continue;\n      }\n\n      budget.syncFromResponse(userId, res);\n\n      if (res.status === 429) {\n        // A 429 is a healthy answer, so it does not trip the breaker — it\n        // consumes the retry budget and then parks the window.\n        const retryAfter =\n          parseRetryAfter(headerOf(res, \"retry-after\"), now()) ??\n          resetHeaderMs(res) ??\n          fullJitterDelay(attempts, { baseDelayMs, maxDelayMs, random });\n\n        if (attempts >= maxAttempts || waitedMs + retryAfter > maxWaitMs) {\n          recordGap({ userId, url: String(url), reason: \"rate-limited\", retryAtMs: now() + retryAfter });\n          return {\n            outcome: \"rate-limited\",\n            response: res,\n            status: 429,\n            attempts,\n            waitedMs,\n            reason: \"rate-limited\",\n            retryAtMs: now() + retryAfter,\n          };\n        }\n        await sleep(retryAfter);\n        waitedMs += retryAfter;\n        continue;\n      }\n\n      if (res.status >= 500) {\n        breaker.recordFailure(key);\n        if (!idempotent) {\n          // The server may have applied it before failing. Replaying a POST\n          // here is how one workout becomes two.\n          recordGap({ userId, url: String(url), reason: \"ambiguous-5xx\", method });\n          return { outcome: \"ambiguous\", ambiguous: true, response: res, status: res.status, attempts, waitedMs, reason: \"non-idempotent\" };\n        }\n        if (attempts >= maxAttempts) {\n          recordGap({ userId, url: String(url), reason: \"server-error\", status: res.status });\n          return { outcome: \"failed\", response: res, status: res.status, attempts, waitedMs, reason: \"server-error\" };\n        }\n        const delay = fullJitterDelay(attempts, { baseDelayMs, maxDelayMs, random });\n        await sleep(delay);\n        waitedMs += delay;\n        continue;\n      }\n\n      // 2xx, 3xx and 4xx are all final. A 404 or a 401 is the caller's problem\n      // to interpret; retrying it just burns the user's quota.\n      breaker.recordSuccess(key);\n      return { outcome: \"ok\", response: res, status: res.status, attempts, waitedMs };\n    }\n  }\n\n  function resetHeaderMs(res) {\n    for (const name of RESET_HEADERS) {\n      const v = headerOf(res, name);\n      if (v !== null && v !== undefined && v !== \"\" && Number.isFinite(Number(v))) {\n        return Number(v) * 1000;\n      }\n    }\n    return null;\n  }\n\n  return {\n    fetchForUser,\n    /** Windows we skipped. Feed these to the reconciliation sweep. */\n    gaps,\n    budgetFor: (userId) => budget.peek(userId),\n    breakerState: (urlOrKey) => breaker.peek(scopeOf(urlOrKey)),\n  };\n}\n```\n\n## How to adapt it\n\n**Add your provider's header names.** `REMAINING_HEADERS` and `RESET_HEADERS` are ordered lists. Put yours at the front rather than replacing the lot, so the wrapper keeps working when you add a second provider with a different convention.\n\n**Set the ceiling from the response, not from a constant.** The `budget.limit` option is only the starting allowance for a user the wrapper has not seen yet. The moment the provider tells you what is left, that number wins. If your provider reports nothing, run your suite at a ceiling low enough that the storm case triggers every time — a job that is correct at ten requests per hour is correct at a thousand, and the reverse is not true.\n\n**Choose the breaker key deliberately.** `scopeOf` defaults to the URL origin, which isolates providers from each other. If one provider's endpoints fail independently, key on origin plus a route family instead. Do not key it on the user: a breaker exists to protect a dependency, and a per-user breaker cannot see that the dependency is down.\n\n**Drain the gaps list.** Every skip and every parked window pushes an entry onto `gaps`, and the `onGap` hook lets you write it somewhere durable. Those entries are the input to the reconciliation sweep that eventually re-reads what you missed; on their own in memory they are a metric, not a recovery. The read model that has to tolerate the hole in the meantime is covered in [missing data and gaps](/architecture/missing-data-and-gaps).\n\n**Mark your writes honestly.** The idempotency default is method-based. If a provider documents a specific `POST` as safe to repeat, opt that one call in explicitly rather than widening the default set, and leave everything else alone.\n\n**Cap the total, not just each call.** `maxWaitMs` refuses to sit on a `Retry-After` longer than your job can afford. A worker that obediently sleeps for the full reset window is a worker that is not doing anything else, and on a per-user quota that is often the whole pool.\n\n## The contract the tests hold it to\n\nEach bullet is one test in `cookbook/rate-limit-fetcher.test.mjs`, run against a fake clock that advances only when the code sleeps.\n\n- A `429` carrying `Retry-After: 120` waits exactly 120,000 milliseconds — not a default that happens to exceed it, and not one millisecond less — then retries once and succeeds.\n- A `429` carrying an HTTP-date `Retry-After` waits the right interval. A parser that only calls `parseInt` fails this test, and the header is permitted in both forms.\n- A `429` with **no** `Retry-After` falls back to jittered exponential backoff. It neither busy-loops nor raises on the missing header, which matters because the specification says a `429` **may** carry the header, not must.\n- A `429` storm that outlasts the retry budget returns a rate-limited outcome with the time to retry, records the gap, and leaves the breaker untouched: a provider answering `429` is healthy.\n- With a budget of two, user A's third call is skipped before `fetch` is called and reports when the window resets, while user B's call goes through on their own bucket. The gap is recorded once.\n- A budget window that rolls over restores the allowance, and a response reporting zero remaining sets the budget to zero immediately — the server's count beats the local counter.\n- Three `503` responses back off with full jitter, and every delay lands inside its own bound: zero is a legitimate draw, so is a value near the exponential ceiling, and the ceiling itself is clamped at the configured maximum. A separate property test draws sixteen hundred delays across eight attempt numbers and asserts each one stays in range.\n- The breaker opens after the configured number of consecutive failures. The next call makes **no** request at all: it returns a skipped outcome with a retry time and records a gap, so the sweep knows which window was never read.\n- After the cooldown, one probe is allowed through, and a good probe closes the circuit. Breakers are per origin, so one dead provider does not stop the others.\n- A `POST` that dies in transit is reported as ambiguous after exactly one attempt, with no sleep and no retry — the server may have applied it. The same holds for a `PATCH` that comes back `502`.\n- The same transport failure on a `GET` **is** retried, and still gives up in finite time with a recorded gap.\n- A `404` is final: one call, no retry, no quota burned on a URL that will keep being wrong.\n\n## What it deliberately does not do\n\nIt does not cache, and caching is the largest single reduction available to most integrations — most historical fitness data never changes. It does not batch or debounce, which is the other big one. And no amount of fault injection can tell you how the real provider enforces its limit: whether the bucket is fixed or sliding, whether repeated violations compound, or whether it degrades by dropping requests instead of answering `429`. What replaces that is one deliberate staging run per provider against a real consented account, driven hard enough to see an actual `429` and record what the headers said."
  },
  {
    "slug": "day-boundary-rollup",
    "primaryQuery": "group health samples by local day code example",
    "h1": "Day-Boundary Rollup: Grouping Samples by Civil Date",
    "metaTitle": "Day-Boundary Rollup: Civil Dates, Not UTC Buckets",
    "metaDescription": "Runnable, dependency-free JavaScript that stamps a civil date at ingest and rolls up on it, so 23- and 25-hour days and travel days stay correct.",
    "updated": "2026-08-12",
    "answer": "A copy-and-run implementation of the civil-date daily rollup: a writer that computes each sample's local date from its instant and its UTC offset at ingest, a rollup that groups strictly on that stored date and refuses to re-derive it at read time, and helpers that report how long a given civil day actually was. Plain modern JavaScript, no dependencies, Node 20 and above, with a node:test suite that injects the offset series and the sample store so nothing touches a clock or a network. The fixed-UTC-window version is exported alongside it, marked as the anti-pattern, so the tests can show exactly which samples it invents on a spring-forward day and which it loses in autumn. The pattern itself is argued on the timezones and day boundaries page; this is the code.",
    "faqs": [
      {
        "q": "Where in a health pipeline should the local date get computed?",
        "a": "At ingest, once, on the write path, and then stored as a real column. The writer in this recipe takes the instant and the UTC offset in effect at that instant and stamps the result onto the row before it reaches the store. Computing it later, in the query, means every daily read has to carry the user's full offset history in scope, gives up a plain index, and makes the answer to what were their steps on the 14th depend on where the user's phone happens to be when they ask. Denormalising one column buys a rollup that is indexable, transition-proof and travel-proof, and the cost is that you have to be willing to recompute the column when you learn an offset was wrong."
      },
      {
        "q": "What should the writer do with a sample that arrives without an offset?",
        "a": "Refuse it, loudly, rather than filling in a value. This implementation throws a TypeError instead of defaulting to UTC or to the device's current zone, because a filled-in offset is indistinguishable from an observed one a week later and it quietly rewrites every trip the user ever took. The practical handling is upstream of the writer: resolve the offset from the platform field, the device, or the recording zone at capture time, record which of those it was in the offsetSource column, and let anything you genuinely cannot resolve fail into a gap you can see. A recorded guess you can identify later is survivable; an invisible one is not."
      },
      {
        "q": "How do I exercise a 23-hour day in tests without waiting for March?",
        "a": "Inject the zone as a piecewise offset series rather than reading a real timezone database. The recipe's createOffsetSeries takes a list of segments, each a transition instant and the offset that takes effect there, and every helper resolves against that. So a spring-forward day is three lines of fixture, a traveller's 16-hour day is two segments built from that one user's own offset history, and the whole suite runs in milliseconds with no clock and no network. Keep the series out of production code, though: it is a stand-in built to reason about one transition, not a replacement for the real rules, which change when governments change them."
      },
      {
        "q": "Can the rollup run against a real database rather than the in-memory store?",
        "a": "Yes, and it is designed for that swap. The writer only ever calls put on whatever store you hand it, so a Postgres upsert keyed on the provider's own record identity drops straight in, and the rollup is a pure function over rows so it maps onto a group-by on the civil-date column. Two properties have to survive the swap. Writes stay idempotent on provider record identity, because a replayed window must be a no-op rather than a doubling. And the rollup write is an update rather than a do-nothing on conflict, or you pin a day to whichever partial value happened to arrive first that morning."
      }
    ],
    "related": [
      {
        "href": "/architecture/timezones-and-day-boundaries",
        "label": "Timezones and day boundaries"
      },
      {
        "href": "/day-boundaries",
        "label": "Live day-boundary demo"
      },
      {
        "href": "/architecture/time-series-storage",
        "label": "Time-series storage for health data"
      },
      {
        "href": "/architecture/incremental-sync",
        "label": "Incremental sync and cursors"
      }
    ],
    "cta": {
      "pitch": "We publish these recipes as runnable, CI-tested files rather than snippets. Our newsletter covers the parts that bite after the code compiles: recomputing a day you already shipped, and the offsets your providers quietly invented for you."
    },
    "body": "A daily total is a calendar question, not a time-range question. The argument for that, the three columns it forces on your schema, and the decision about whose midnight defines the day all belong to [timezones and day boundaries](/architecture/timezones-and-day-boundaries); the [live day-boundary demo](/day-boundaries) lets you watch a civil day stop being 24 hours long in your own browser. This page is the code that falls out of those two.\n\n## What the recipe does\n\nThree moving parts, and the split between them is the whole point.\n\n- **A writer** that stamps `civilDate` onto every sample at ingest, from the instant and the UTC offset in effect at that instant. It refuses to store a sample whose offset it does not know rather than defaulting to UTC, because a defaulted offset silently rewrites the user's travel history and nothing downstream can tell that you guessed.\n- **A rollup** that groups strictly on the stored `civilDate` and throws if a row does not have one. It will not re-derive the date at read time. That single refusal is what makes the daily figure stable when the user's phone changes zone.\n- **Civil-day helpers** that answer how long a given civil day actually was, given a piecewise offset series: 23 hours on a spring-forward day, 25 on a fall-back day, and 16 or 32 on a travel day if you feed them one user's real offset history.\n\nPlus the anti-pattern, exported on purpose: a rollup that buckets on a fixed 24-hour UTC window. It is there so the test suite can measure exactly which samples it invents and which it loses, rather than asserting in the abstract that it is bad.\n\n## The implementation\n\nThis is the canonical file. It lives in the repo at `cookbook/day-boundary-rollup.mjs` and its test file runs in CI on every commit, so what you are reading is what passes.\n\n```js\n/**\n * day-boundary-rollup.mjs\n *\n * WHAT THIS IMPLEMENTS\n *   Civil-date daily rollups for health samples. Every sample is stored as\n *   { utcInstant, utcOffsetMinutes, civilDate } where `civilDate` is computed\n *   from instant + offset at WRITE time and is never re-derived at read time.\n *   The rollup groups strictly on the stored `civilDate`, so it is correct\n *   across DST transitions (a civil day is 23, 24 or 25 hours long) and across\n *   travel (each sample carries the offset the user actually experienced).\n *\n *   Also included, deliberately, is the anti-pattern: a rollup that buckets on\n *   a fixed 24-hour UTC window. It is exported so tests can show precisely\n *   which samples it drops and which it double-counts.\n *\n * WHICH aifitnessapi.com PAGE DOCUMENTS THE PATTERN\n *   https://aifitnessapi.com/architecture/timezones-and-day-boundaries\n *   https://aifitnessapi.com/day-boundaries  (interactive demo of the same bug)\n *   https://aifitnessapi.com/cookbook/day-boundary-rollup  (this recipe)\n *\n * Node 20+. Zero runtime dependencies. No I/O, no clock reads: the store and\n * the offset series are injected.\n *\n * MIT — from aifitnessapi.com/cookbook\n */\n\nconst MS_PER_MINUTE = 60_000;\nconst MS_PER_HOUR = 3_600_000;\nconst MS_PER_DAY = 86_400_000;\n\n/**\n * Day-rule identifiers. The rule that produced a `civilDate` is stored on the\n * row, because you will change the rule at least once and you need to know\n * which rows were written under the old one.\n */\nexport const DAY_RULE = Object.freeze({\n  /** The zone offset in effect at each sample's own timestamp. The default. */\n  SAMPLE_OFFSET: \"sample-offset/v1\",\n  /** A fixed home or profile zone applied to every sample. */\n  FIXED_HOME_ZONE: \"fixed-home-zone/v1\",\n});\n\n/** How the offset on a sample was obtained. Confidence, not correctness. */\nexport const OFFSET_SOURCE = Object.freeze({\n  PROVIDER: \"provider\",\n  DEVICE_REPORTED: \"device-reported\",\n  INFERRED: \"inferred\",\n});\n\n// ---------------------------------------------------------------------------\n// Civil-date arithmetic\n// ---------------------------------------------------------------------------\n\nfunction pad2(n) {\n  return String(n).padStart(2, \"0\");\n}\n\n/**\n * Format the UTC calendar date of an instant as YYYY-MM-DD.\n * @param {number} instantMs\n * @returns {string}\n */\nexport function utcDateOf(instantMs) {\n  const d = new Date(instantMs);\n  return `${d.getUTCFullYear()}-${pad2(d.getUTCMonth() + 1)}-${pad2(d.getUTCDate())}`;\n}\n\n/**\n * The civil (local) date a sample belongs to, from its instant and the UTC\n * offset in effect at that instant. This is the only place the derivation\n * happens; callers store the result rather than recomputing it.\n *\n * @param {number} instantMs epoch milliseconds\n * @param {number} utcOffsetMinutes minutes east of UTC (e.g. -300 for EST)\n * @returns {string} YYYY-MM-DD\n */\nexport function civilDateFrom(instantMs, utcOffsetMinutes) {\n  if (!Number.isFinite(instantMs)) throw new TypeError(\"instantMs must be a finite number\");\n  if (!Number.isFinite(utcOffsetMinutes)) {\n    // Never default a missing offset to UTC. A guessed offset rewrites the\n    // user's travel history and there is no way to tell later that you guessed.\n    throw new TypeError(\"utcOffsetMinutes must be a finite number; refuse to default it\");\n  }\n  return utcDateOf(instantMs + utcOffsetMinutes * MS_PER_MINUTE);\n}\n\n/** Midnight UTC of a YYYY-MM-DD string, in epoch milliseconds. */\nexport function utcMidnightOf(civilDate) {\n  const m = /^(\\d{4})-(\\d{2})-(\\d{2})$/.exec(civilDate);\n  if (!m) throw new TypeError(`not a YYYY-MM-DD civil date: ${civilDate}`);\n  return Date.UTC(Number(m[1]), Number(m[2]) - 1, Number(m[3]));\n}\n\n/** The civil date after this one. Successor arithmetic, never \"+ 24 hours\". */\nexport function nextCivilDate(civilDate) {\n  return utcDateOf(utcMidnightOf(civilDate) + MS_PER_DAY);\n}\n\n// ---------------------------------------------------------------------------\n// Offset series — a piecewise-constant model of one zone's UTC offset\n// ---------------------------------------------------------------------------\n\n/**\n * Build an offset series. This is a deliberately tiny stand-in for a tz\n * database: enough to reason about a specific transition in a test, not a\n * replacement for the real rules. Production code resolves offsets from the\n * platform (Intl / ZoneRules / the provider's own field) and stores the result.\n *\n * @param {Array<{from: number|null, offsetMinutes: number}>} segments\n *   `from` is the instant the offset takes effect; `null` means \"since forever\".\n *   Segments are sorted ascending internally.\n */\nexport function createOffsetSeries(segments) {\n  if (!Array.isArray(segments) || segments.length === 0) {\n    throw new TypeError(\"offset series needs at least one segment\");\n  }\n  const sorted = segments\n    .map((s) => ({ from: s.from == null ? -Infinity : s.from, offsetMinutes: s.offsetMinutes }))\n    .sort((a, b) => a.from - b.from);\n  if (sorted[0].from !== -Infinity) sorted[0] = { ...sorted[0], from: -Infinity };\n\n  return {\n    segments: sorted,\n    /** Offset in effect at an instant. Transitions are inclusive of `from`. */\n    offsetAt(instantMs) {\n      let out = sorted[0].offsetMinutes;\n      for (const seg of sorted) {\n        if (instantMs >= seg.from) out = seg.offsetMinutes;\n        else break;\n      }\n      return out;\n    },\n    /** Distinct offsets that ever apply. */\n    offsets() {\n      return [...new Set(sorted.map((s) => s.offsetMinutes))];\n    },\n    /** Transition instants (excluding the -Infinity sentinel). */\n    transitions() {\n      return sorted.map((s) => s.from).filter((f) => Number.isFinite(f));\n    },\n  };\n}\n\n/**\n * The first instant of a civil date under an offset series.\n *\n * Normally this is `utcMidnight(date) - offset`, checked against the offset\n * actually in effect there. Where a zone shifts its clocks at midnight itself,\n * local midnight may not exist; then the day starts at the transition instant.\n *\n * @returns {number} epoch milliseconds\n */\nexport function startOfCivilDayUtc(civilDate, series) {\n  const base = utcMidnightOf(civilDate);\n  const candidates = [];\n  for (const offsetMinutes of series.offsets()) {\n    const t = base - offsetMinutes * MS_PER_MINUTE;\n    if (series.offsetAt(t) === offsetMinutes) candidates.push(t);\n  }\n  if (candidates.length > 0) return Math.min(...candidates);\n\n  // Local midnight was skipped by a transition. The day begins at the first\n  // transition instant that already reads as this civil date.\n  for (const t of series.transitions().sort((a, b) => a - b)) {\n    if (civilDateFrom(t, series.offsetAt(t)) === civilDate) return t;\n  }\n  throw new RangeError(`civil date ${civilDate} does not occur in this offset series`);\n}\n\n/**\n * How long a civil day actually lasts, in hours. 23 on a spring-forward day,\n * 25 on a fall-back day, 24 the rest of the year — and 16 or 32 on a travel\n * day, if you feed it a series built from one user's real offset history.\n */\nexport function civilDayLengthHours(civilDate, series) {\n  const start = startOfCivilDayUtc(civilDate, series);\n  const end = startOfCivilDayUtc(nextCivilDate(civilDate), series);\n  return (end - start) / MS_PER_HOUR;\n}\n\n/** `{ civilDate, startUtc, endUtc, hours }` — the half-open bounds of a civil day. */\nexport function civilDayBoundsUtc(civilDate, series) {\n  const startUtc = startOfCivilDayUtc(civilDate, series);\n  const endUtc = startOfCivilDayUtc(nextCivilDate(civilDate), series);\n  return { civilDate, startUtc, endUtc, hours: (endUtc - startUtc) / MS_PER_HOUR };\n}\n\n// ---------------------------------------------------------------------------\n// Write path — civilDate is stamped here, once\n// ---------------------------------------------------------------------------\n\n/** Minimal injectable store. Swap for your real table; the writer only calls put(). */\nexport function createMemorySampleStore() {\n  const rows = [];\n  const seen = new Set();\n  return {\n    /** Idempotent on (provider, externalId): a replayed window is a no-op. */\n    put(record) {\n      const key = `${record.provider}\u0000${record.externalId}`;\n      if (seen.has(key)) return false;\n      seen.add(key);\n      rows.push(record);\n      return true;\n    },\n    all() {\n      return rows.slice();\n    },\n    get size() {\n      return rows.length;\n    },\n  };\n}\n\n/**\n * A writer that computes the civil date once, at ingest, and refuses to store\n * a sample whose offset it does not know.\n *\n * @param {{store: {put: Function}, dayRule?: string}} deps\n */\nexport function createSampleWriter({ store, dayRule = DAY_RULE.SAMPLE_OFFSET }) {\n  if (!store || typeof store.put !== \"function\") throw new TypeError(\"store.put is required\");\n\n  return {\n    dayRule,\n    /**\n     * @param {{\n     *   provider: string, externalId: string, metric: string, value: number,\n     *   utcInstant: number|string|Date, utcOffsetMinutes: number,\n     *   zoneId?: string, offsetSource?: string\n     * }} sample\n     */\n    write(sample) {\n      const utcInstant =\n        sample.utcInstant instanceof Date\n          ? sample.utcInstant.getTime()\n          : typeof sample.utcInstant === \"string\"\n            ? Date.parse(sample.utcInstant)\n            : sample.utcInstant;\n\n      const record = Object.freeze({\n        provider: sample.provider,\n        externalId: sample.externalId,\n        metric: sample.metric,\n        value: sample.value,\n        // Three columns, not one. All written here.\n        utcInstant,\n        utcOffsetMinutes: sample.utcOffsetMinutes,\n        civilDate: civilDateFrom(utcInstant, sample.utcOffsetMinutes),\n        zoneId: sample.zoneId ?? null,\n        offsetSource: sample.offsetSource ?? OFFSET_SOURCE.PROVIDER,\n        dayRule,\n      });\n\n      store.put(record);\n      return record;\n    },\n  };\n}\n\n// ---------------------------------------------------------------------------\n// Read path — group on the stored civil date, recompute, never increment\n// ---------------------------------------------------------------------------\n\n/**\n * The correct rollup. Groups strictly on the stored `civilDate`; it will throw\n * rather than re-derive a missing one, because re-deriving at read time is the\n * bug this recipe exists to prevent.\n *\n * Pure function of its input: recompute a cell to answer \"is this number\n * right?\", never increment a counter.\n *\n * @param {Array<object>} records\n * @returns {Array<{civilDate: string, total: number, count: number, sampleIds: string[]}>}\n */\nexport function rollupByCivilDate(records) {\n  const buckets = new Map();\n  for (const r of records) {\n    if (typeof r.civilDate !== \"string\") {\n      throw new TypeError(\n        `sample ${r.externalId} has no stored civilDate; the rollup will not derive one at read time`,\n      );\n    }\n    bump(buckets, r.civilDate, r);\n  }\n  return finish(buckets);\n}\n\n/**\n * ANTI-PATTERN — DO NOT SHIP. Included only so a test can measure the damage.\n *\n * Buckets samples into fixed 24-hour UTC windows, i.e. `date(utcInstant)`. This\n * is what a `time_bucket('1 day', ts)` continuous aggregate does, and what any\n * \"group by date(timestamp)\" query does. It is wrong for every user outside\n * UTC, every day; on DST days it drops or double-counts a whole hour; and for\n * a traveller it files activity under the day they were not living in.\n *\n * @returns {Array<{civilDate: string, total: number, count: number, sampleIds: string[]}>}\n */\nexport function rollupByFixedUtcWindowAntiPattern(records) {\n  const buckets = new Map();\n  for (const r of records) bump(buckets, utcDateOf(r.utcInstant), r);\n  return finish(buckets);\n}\n\nfunction bump(buckets, key, r) {\n  let b = buckets.get(key);\n  if (!b) {\n    b = { civilDate: key, total: 0, count: 0, sampleIds: [] };\n    buckets.set(key, b);\n  }\n  b.total += r.value;\n  b.count += 1;\n  b.sampleIds.push(r.externalId);\n}\n\nfunction finish(buckets) {\n  return [...buckets.values()].sort((a, b) => (a.civilDate < b.civilDate ? -1 : 1));\n}\n\n/**\n * Every sample the anti-pattern files under a date the user did not experience.\n * @returns {Array<{externalId: string, civilDate: string, utcBucket: string}>}\n */\nexport function misfiledByUtcWindow(records) {\n  const out = [];\n  for (const r of records) {\n    const utcBucket = utcDateOf(r.utcInstant);\n    if (utcBucket !== r.civilDate) {\n      out.push({ externalId: r.externalId, civilDate: r.civilDate, utcBucket });\n    }\n  }\n  return out;\n}\n\n/**\n * Line up the correct rollup against the anti-pattern one.\n *\n * `delta` is anti − correct: positive means the UTC window double-counted\n * activity into that date, negative means it dropped activity out of it.\n */\nexport function compareRollups(correct, antiPattern) {\n  const byDate = new Map();\n  for (const b of correct) {\n    byDate.set(b.civilDate, {\n      civilDate: b.civilDate,\n      correctTotal: b.total,\n      correctCount: b.count,\n      antiTotal: 0,\n      antiCount: 0,\n    });\n  }\n  for (const b of antiPattern) {\n    const row = byDate.get(b.civilDate) ?? {\n      civilDate: b.civilDate,\n      correctTotal: 0,\n      correctCount: 0,\n      antiTotal: 0,\n      antiCount: 0,\n    };\n    row.antiTotal = b.total;\n    row.antiCount = b.count;\n    byDate.set(b.civilDate, row);\n  }\n  return [...byDate.values()]\n    .sort((a, b) => (a.civilDate < b.civilDate ? -1 : 1))\n    .map((r) => ({ ...r, delta: r.antiTotal - r.correctTotal }));\n}\n```\n\n## Adapting it\n\n**Offsets are seconds in most real schemas, minutes here.** The recipe uses minutes because that is what most SDKs hand you, but not every zone is a whole number of hours from UTC and an hours-valued column corrupts the users who live in the ones that are not. If your storage column is seconds, convert at the writer boundary and keep the column as the wider type.\n\n**Swap the store, keep the writer.** `createMemorySampleStore` exists so the tests need no database. In production the writer only calls `put`, so a Postgres upsert keyed on the provider's own record identity drops straight in. Keep the idempotency: a replayed window has to be a no-op, because at-least-once is the only delivery a resumable ingest can offer.\n\n**The offset series is a stand-in, not a tz database.** `createOffsetSeries` models one zone's offset as piecewise-constant segments, which is enough to reason about a specific transition in a test and nowhere near enough for production. Resolve real offsets from the platform, the provider field, or the device, record which of those it was, and store the answer. Treat the tz database as a dependency with a release cadence, not as a constant.\n\n**Carry the day rule as a column.** `DAY_RULE.SAMPLE_OFFSET` is the default here: the day the user actually lived through. If your product needs a fixed home zone instead — a coaching product where a week is a training block — that is a legitimate second rule, and versioning it on the row is the difference between a recompute and an archaeology project when you change your mind. Same machinery as [metric versioning and recompute](/architecture/metric-versioning-and-recompute).\n\n**The rollup is a pure function, deliberately.** It recomputes from raw samples every time rather than incrementing a counter. That is what lets you answer \"is this number right?\" by recomputing one cell. The storage layer that makes recomputing cheap at scale — partitioning, refresh, the dirty-day queue — is [time-series storage for health data](/architecture/time-series-storage), and the queue that decides which cells to recompute is [incremental sync](/architecture/incremental-sync).\n\n## The test contract\n\nThe suite in `cookbook/day-boundary-rollup.test.mjs` asserts:\n\n- **A spring-forward civil day holds 23 hours of samples and still rolls up as one date.** `2026-03-08` in America/New_York runs from `2026-03-08T05:00:00Z` to `2026-03-09T04:00:00Z`. Hourly samples across it produce 23 rows, two distinct offsets, no local 02:00 anywhere, and exactly one rollup bucket.\n- **A fall-back day holds 25, and local 01:00 appears twice at two different instants** with two different offsets — the case that makes \"just store local time\" unrecoverable rather than merely lazy.\n- **A traveller's samples land on the civil date they experienced.** Six samples across a LAX-to-LHR flight roll up as 2, 3 and 1 across three civil dates. Under a UTC window the traveller's 10 June does not exist at all.\n- **The traveller's 11 June is 16 hours long**, computed from their own offset history, with the day starting at `2026-06-11T07:00:00Z`.\n- **The anti-pattern's error is exact, not approximate.** On the 23-hour day the UTC window counts 24 hours: it drops the user's last four EDT hours into the 9th and pulls five hours of the 7th in, for a net of one invented hour. On the 25-hour day it drops five and pulls in four, for a net of one lost hour. The test asserts the individual sample identities, not just the totals.\n- **The rollup throws on a row with no stored civil date**, and the anti-pattern cheerfully derives one — which is how it gets shipped.\n- **Writes are idempotent on provider record identity**, so a replayed window leaves the total unchanged.\n\n## What this does not fix\n\nIt does not make the offset true. It makes it recorded, which is a smaller claim. A user who never changes their phone's timezone while travelling hands you a confidently wrong offset and nothing in the data reveals it; that is what the `offsetSource` field is for, and it lets you tell those rows apart later rather than making them correct.\n\nIt also does nothing about samples that arrive days late or get retro-edited by the platform months on. Those change which days are dirty, not how a day is defined. The three columns are what make the repair cheap when it happens."
  },
  {
    "slug": "rep-counter",
    "primaryQuery": "rep counting state machine code example javascript",
    "h1": "Rep Counter: A State Machine and the Scorer That Guards It",
    "metaTitle": "Rep Counter Code: Hysteresis FSM Plus a Scorer",
    "metaDescription": "Runnable JavaScript rep counter: EMA smoothing, hysteresis, debounce and rep events, plus a per-clip precision and recall scorer with no aggregates.",
    "updated": "2026-08-12",
    "answer": "A copy-and-run rep counter and the scoring harness that keeps it honest. The counter is a two-phase finite state machine over one smoothed joint angle, with an injectable EMA constant, separate up and down entry thresholds so jitter at one boundary cannot double-fire, a minimum phase duration that rejects a spike rather than delaying it, a confidence gate, and rep events emitted with timestamps. The scorer matches predicted rep timestamps one-to-one and greedily against labelled ground truth inside a tolerance window and reports precision and recall per clip, with no aggregate and no F-score anywhere, because both let a miss and a phantom cancel out. Plain modern JavaScript, no dependencies, Node 20 and above, with a node:test suite that runs on synthetic angle streams rather than a camera.",
    "faqs": [
      {
        "q": "What smoothing constant should the EMA use for a joint angle?",
        "a": "There is no right value to copy, which is why it is a constructor argument here rather than a baked-in number. It is a straight trade: a heavier filter kills jitter and adds lag, and the lag is visible in this recipe's own end-to-end test as a systematic 52 millisecond delay on a two-second rep. Pick it against your frame rate and your movement tempo, then hold it still and let the corpus tell you when a change was worth it. Two practical notes. Setting it to 1 disables smoothing entirely, which is useful in tests where you want raw noise to reach the state machine. And if your signal is too noisy for an exponential average to handle, replace the filter behind the same seam rather than loosening the thresholds to compensate."
      },
      {
        "q": "How do I choose the two hysteresis thresholds for a new exercise?",
        "a": "Start from the joint whose angle swings most cleanly through the movement in the camera plane, then set the entry thresholds inside each extreme rather than at it, leaving a gap wide enough that ordinary keypoint noise cannot cross both. The gap is the hysteresis and it is the only thing preventing a double-count. This implementation throws at construction time if the two values are equal or inverted, so a mistake surfaces in your setup code rather than as a miscount in the field. Then calibrate: range of motion varies by body and mobility, so a pair that works for one person may never trigger for another, and the usual fix is a reference rep captured at the start of a set. Build the counter per set once you know the user's range."
      },
      {
        "q": "Can I score the counter against keypoints my own pose model produced?",
        "a": "Replaying recorded keypoint sequences captured from a real device run is exactly right, and this recipe is built for it: every frame carries its own timestamp, nothing reads a clock, so a whole corpus scores in milliseconds and the state machine regression-tests cheaply. What is circular is generating the clips themselves from the same pose model you are testing. That scores your state machine against your own model's opinion, and catching the cases where that opinion is wrong is the entire reason the corpus exists. The test is honest when the thing under test sits downstream of the recording, and dishonest when it is the recorder."
      },
      {
        "q": "Why does the scorer return a null precision instead of a perfect score?",
        "a": "Because a clip where nothing was predicted has no evidence about precision, and reporting 1.0 there would let a counter that fired zero times look flawless. The scorer returns null for a metric whose denominator is empty, and the same reasoning drives the rest of its output: no F-score, because collapsing the two lets a gain in one silently pay for a regression in the other, and no pooled figure across clips, because a pooled number is dominated by whichever exercise you filmed the most of. Under-counting and over-counting also do not feel remotely alike to a user, so they get two floors rather than one target. Gate the build on the per-clip diff instead, and let an improvement fail too, so accepting it means committing an updated baseline in the same change."
      }
    ],
    "related": [
      {
        "href": "/motion/how-rep-counting-works",
        "label": "How rep counting works"
      },
      {
        "href": "/test/rep-counting",
        "label": "Testing a rep counting algorithm"
      },
      {
        "href": "/guides/add-rep-counting",
        "label": "Add rep counting"
      },
      {
        "href": "/motion/pose-estimation-accuracy",
        "label": "Pose estimation accuracy"
      }
    ],
    "cta": {
      "pitch": "Pose models and SDKs ship on their own schedule, and a version bump moves a rep counter without touching a line of your code. We track what changes for teams shipping camera fitness features, and publish the harnesses that catch it."
    },
    "body": "Two things that belong together and are usually written apart: a rep-counting state machine, and the scorer that proves it still works after you touch its thresholds. The algorithm — why you reduce a pose to one oscillating scalar, why hysteresis beats a single threshold — is [how rep counting works](/motion/how-rep-counting-works). Why a final-count assertion is worthless and what belongs in a labelled corpus is [testing a rep counting algorithm](/test/rep-counting). This page is the runnable version of both.\n\n## What the recipe does\n\n**The counter** is a two-phase finite state machine over one smoothed joint angle, with four gates:\n\n- **EMA smoothing** with an injectable alpha. Never run threshold logic on raw keypoints; alpha is a trade between lag and jitter, so it is a parameter rather than a constant.\n- **Hysteresis.** Separate entry thresholds for the up and down phases. The constructor throws if you pass a single threshold or an inverted pair, because that is the double-counting bug and it should not survive to the field.\n- **A minimum phase duration.** Note the semantics: a cycle whose up phase was shorter than the gate is rejected outright, not merely delayed. Blocking a transition and then firing it 250 ms later is still a wrong count.\n- **A minimum amplitude and a confidence floor.** Low-confidence frames are skipped before the smoother sees them, so an occluded joint does not poison the signal for the next second.\n\nReps are emitted as timestamped events at the return crossing, which is the moment the counter is contractually supposed to increment. Write that into your labelling policy — if truth marks the bottom of the movement and your prediction marks the top, every match either fails or succeeds by luck.\n\n**The scorer** treats the counter as a classifier. Predicted rep timestamps are matched one-to-one and greedily against labelled ground truth inside a tolerance window, producing true positives, false positives and false negatives, and from those precision and recall per clip. There is no aggregate figure and no F-score anywhere in the file, on purpose: both let a gain in one error mode silently pay for a regression in the other.\n\n## The implementation\n\nThis is the canonical file. It lives in the repo at `cookbook/rep-counter.mjs` and its test file runs in CI on every commit, so what you are reading is what passes.\n\n```js\n/**\n * rep-counter.mjs\n *\n * WHAT THIS IMPLEMENTS\n *   Two things a camera fitness feature needs and that are usually written\n *   badly together:\n *\n *   1. A rep-counting finite state machine over a smoothed joint-angle stream.\n *      EMA smoothing with an injectable alpha, hysteresis (separate entry\n *      thresholds for the up and down phases, so jitter at one boundary cannot\n *      double-fire), a minimum phase duration, a minimum amplitude gate, a\n *      confidence gate, and rep events emitted with timestamps.\n *\n *   2. A scorer that treats the counter as a classifier: predicted rep\n *      timestamps matched one-to-one and greedily against labelled ground truth\n *      inside a tolerance window, producing precision and recall PER CLIP.\n *      There is deliberately no aggregate-count comparison and no F-score,\n *      because both let a miss and a phantom cancel each other out.\n *\n * WHICH aifitnessapi.com PAGES DOCUMENT THE PATTERN\n *   https://aifitnessapi.com/motion/how-rep-counting-works   (the state machine)\n *   https://aifitnessapi.com/test/rep-counting               (the scoring gate)\n *   https://aifitnessapi.com/cookbook/rep-counter            (this recipe)\n *\n * Node 20+. Zero runtime dependencies. No timers and no clock reads: every\n * frame carries its own timestamp, so a recorded keypoint sequence replays\n * deterministically.\n *\n * MIT — from aifitnessapi.com/cookbook\n */\n\n// ---------------------------------------------------------------------------\n// Smoothing\n// ---------------------------------------------------------------------------\n\n/**\n * Exponential moving average. `alpha` is the weight of the newest sample:\n * 1 means no smoothing at all, small values mean heavy smoothing and more lag.\n *\n * Smoothing is not optional — never run threshold logic on raw keypoints — but\n * it is a trade, so the constant is injected rather than baked in.\n */\nexport function createEmaSmoother({ alpha }) {\n  if (!(alpha > 0 && alpha <= 1)) throw new RangeError(\"alpha must be in (0, 1]\");\n  let value = null;\n  return {\n    get value() {\n      return value;\n    },\n    push(x) {\n      value = value === null ? x : alpha * x + (1 - alpha) * value;\n      return value;\n    },\n    reset() {\n      value = null;\n    },\n  };\n}\n\n// ---------------------------------------------------------------------------\n// The state machine\n// ---------------------------------------------------------------------------\n\nexport const PHASE = Object.freeze({ DOWN: \"down\", UP: \"up\" });\n\n/**\n * A rep counter for one exercise on one joint angle.\n *\n * Convention follows the curl worked through on /motion/how-rep-counting-works:\n * a large angle is the extended, resting position (`down`) and a small angle is\n * the contracted position (`up`). A rep is counted on the COMPLETED cycle — the\n * moment the signal returns past `enterDownAbove` — not on either crossing\n * alone. For an exercise where the extremes are the other way round, feed in\n * the negated angle or an angle measured at the opposite joint; the state\n * machine does not care what the number means.\n *\n * @param {object} cfg\n * @param {number} cfg.enterUpBelow      cross below this to enter the up phase\n * @param {number} cfg.enterDownAbove    cross above this to complete the rep\n * @param {number} [cfg.alpha=0.4]       EMA smoothing weight\n * @param {number} [cfg.minPhaseMs=0]    debounce. The down phase must have lasted this long\n *   before it may end, and a cycle whose up phase was shorter than this is rejected as a\n *   spike rather than merely delayed — a delayed count is still a wrong count.\n * @param {number} [cfg.minAmplitude=0]  the cycle must span at least this much smoothed range\n * @param {number} [cfg.minConfidence=0] frames below this are skipped entirely\n */\nexport function createRepCounter(cfg) {\n  const {\n    enterUpBelow,\n    enterDownAbove,\n    alpha = 0.4,\n    minPhaseMs = 0,\n    minAmplitude = 0,\n    minConfidence = 0,\n  } = cfg;\n\n  if (!Number.isFinite(enterUpBelow) || !Number.isFinite(enterDownAbove)) {\n    throw new TypeError(\"enterUpBelow and enterDownAbove are required numbers\");\n  }\n  if (!(enterUpBelow < enterDownAbove)) {\n    // A single threshold, or an inverted pair, is the double-counting bug.\n    // Refuse at construction time rather than miscount in the field.\n    throw new RangeError(\n      \"enterUpBelow must be strictly below enterDownAbove; the gap between them is the hysteresis\",\n    );\n  }\n\n  const ema = createEmaSmoother({ alpha });\n\n  let phase = null;\n  let phaseStartT = null;\n  let phaseMin = Infinity;\n  let phaseMax = -Infinity;\n  let count = 0;\n  let skipped = 0;\n  const events = [];\n  const rejected = [];\n\n  function beginPhase(next, t, seed) {\n    phase = next;\n    phaseStartT = t;\n    phaseMin = seed;\n    phaseMax = seed;\n  }\n\n  return {\n    get count() {\n      return count;\n    },\n    get phase() {\n      return phase;\n    },\n    /** Rep events emitted so far: `{ index, t, amplitude, peakAngle, troughAngle }`. */\n    get events() {\n      return events.slice();\n    },\n    /** Frames dropped by the confidence gate. Worth surfacing to the user. */\n    get skippedFrames() {\n      return skipped;\n    },\n    /** Cycles the gates threw away: `{ t, reason, upPhaseMs, amplitude }`. */\n    get rejected() {\n      return rejected.slice();\n    },\n    /** Predicted rep timestamps, the shape the scorer below wants. */\n    repTimestamps() {\n      return events.map((e) => e.t);\n    },\n\n    /**\n     * Feed one frame.\n     * @param {{t: number, angle: number, confidence?: number}} frame\n     * @returns {Array<object>} rep events emitted by THIS frame (0 or 1)\n     */\n    push({ t, angle, confidence = 1 }) {\n      if (!Number.isFinite(t) || !Number.isFinite(angle)) {\n        throw new TypeError(\"frame needs a finite t and angle\");\n      }\n      if (confidence < minConfidence) {\n        // Occluded or low-confidence joints produce a garbage angle. Skipping\n        // the frame is right; feeding it to the smoother is not.\n        skipped += 1;\n        return [];\n      }\n\n      const s = ema.push(angle);\n\n      if (phase === null) {\n        beginPhase(s <= enterUpBelow ? PHASE.UP : PHASE.DOWN, t, s);\n        return [];\n      }\n\n      if (s < phaseMin) phaseMin = s;\n      if (s > phaseMax) phaseMax = s;\n\n      const phaseMs = t - phaseStartT;\n\n      if (phase === PHASE.DOWN) {\n        // Hysteresis: only a crossing of the LOW threshold opens a new cycle,\n        // so jitter around the high one cannot re-fire. The time gate is a\n        // debounce against re-entering immediately after a counted rep.\n        if (s < enterUpBelow && phaseMs >= minPhaseMs) beginPhase(PHASE.UP, t, s);\n        return [];\n      }\n\n      // phase === UP: the rep completes only on the return crossing, which is\n      // the moment the counter is contractually supposed to increment. Label\n      // your ground truth at the same moment or every match is luck.\n      if (s > enterDownAbove) {\n        const amplitude = s - phaseMin;\n        const trough = phaseMin;\n        beginPhase(PHASE.DOWN, t, s);\n\n        if (phaseMs < minPhaseMs) {\n          rejected.push({ t, reason: \"phase_too_short\", upPhaseMs: phaseMs, amplitude });\n          return [];\n        }\n        if (amplitude < minAmplitude) {\n          rejected.push({ t, reason: \"amplitude_too_small\", upPhaseMs: phaseMs, amplitude });\n          return [];\n        }\n\n        count += 1;\n        const event = Object.freeze({\n          index: count,\n          t,\n          amplitude,\n          peakAngle: s,\n          troughAngle: trough,\n        });\n        events.push(event);\n        return [event];\n      }\n      return [];\n    },\n\n    /**\n     * Drain a whole recorded sequence. Replaying captured keypoints is the\n     * cheap way to regression-test the state machine; scoring the pose model\n     * against its own output is not a test at all.\n     */\n    pushAll(frames) {\n      for (const f of frames) this.push(f);\n      return this.events;\n    },\n\n    reset() {\n      ema.reset();\n      phase = null;\n      phaseStartT = null;\n      phaseMin = Infinity;\n      phaseMax = -Infinity;\n      count = 0;\n      skipped = 0;\n      events.length = 0;\n      rejected.length = 0;\n    },\n  };\n}\n\n// ---------------------------------------------------------------------------\n// Scoring — the counter is a classifier, so score it like one\n// ---------------------------------------------------------------------------\n\n/**\n * Guard the tolerance window against being wide enough to be meaningless.\n *\n * If the window exceeds half the closest gap between two labelled reps, one\n * prediction sits within range of two labels and the score stops measuring\n * anything. This is the check that stops your fastest clip — the one you most\n * want in the corpus — going quietly unscoreable the day someone widens the\n * window to make a flaky clip go green.\n *\n * @throws {RangeError}\n */\nexport function assertToleranceIsMeaningful(truth, toleranceMs) {\n  const ordered = [...truth].sort((a, b) => a - b);\n  for (let i = 1; i < ordered.length; i++) {\n    const gap = ordered[i] - ordered[i - 1];\n    if (gap <= 2 * toleranceMs) {\n      throw new RangeError(\n        `tolerance ${toleranceMs}ms exceeds half the closest labelled rep pair (${gap}ms apart); ` +\n          \"one prediction could satisfy two labels and the score would be meaningless\",\n      );\n    }\n  }\n  return true;\n}\n\n/**\n * One-to-one greedy nearest matching of predicted rep events to labelled ones.\n *\n * Walks the labels in time order; for each, takes the nearest unmatched\n * prediction inside the window and consumes it. Consuming is what makes a\n * double-count show up as a false positive instead of vanishing.\n *\n * @param {number[]} truth      labelled rep timestamps (ms)\n * @param {number[]} predicted  rep timestamps your counter emitted (ms)\n * @param {{toleranceMs: number}} opts\n * @returns {{tp:number, fp:number, fn:number, precision:number, recall:number,\n *            matches: Array<{truth:number, predicted:number, errorMs:number}>,\n *            falsePositives:number[], falseNegatives:number[]}}\n */\nexport function scoreRepEvents(truth, predicted, { toleranceMs }) {\n  if (!Number.isFinite(toleranceMs) || toleranceMs < 0) {\n    throw new TypeError(\"toleranceMs must be a non-negative number\");\n  }\n  assertToleranceIsMeaningful(truth, toleranceMs);\n\n  const unmatched = [...predicted].sort((a, b) => a - b);\n  const matches = [];\n  const falseNegatives = [];\n\n  for (const t of [...truth].sort((a, b) => a - b)) {\n    let bestIdx = -1;\n    let bestErr = Infinity;\n    for (let i = 0; i < unmatched.length; i++) {\n      const err = Math.abs(unmatched[i] - t);\n      if (err <= toleranceMs && err < bestErr) {\n        bestErr = err;\n        bestIdx = i;\n      }\n    }\n    if (bestIdx === -1) {\n      falseNegatives.push(t);\n    } else {\n      const [hit] = unmatched.splice(bestIdx, 1);\n      matches.push({ truth: t, predicted: hit, errorMs: hit - t });\n    }\n  }\n\n  const tp = matches.length;\n  const fp = unmatched.length;\n  const fn = falseNegatives.length;\n  return {\n    tp,\n    fp,\n    fn,\n    // An empty denominator is \"no evidence\", not \"perfect\". Report null.\n    precision: tp + fp === 0 ? null : tp / (tp + fp),\n    recall: tp + fn === 0 ? null : tp / (tp + fn),\n    matches,\n    falsePositives: unmatched,\n    falseNegatives,\n  };\n}\n\n/**\n * Score a whole corpus. Per clip, and only per clip.\n *\n * There is no aggregate here on purpose: a pooled precision figure is dominated\n * by whichever exercise you filmed most of, and a change that breaks two clips\n * while fixing two others leaves it perfectly still.\n *\n * @param {Array<{clip: string, exercise?: string, tags?: string[],\n *                truth: number[], predicted: number[]}>} clips\n * @param {{toleranceMs: number}} opts\n */\nexport function scoreCorpus(clips, { toleranceMs }) {\n  const results = {};\n  for (const c of clips) {\n    const s = scoreRepEvents(c.truth, c.predicted, { toleranceMs });\n    results[c.clip] = {\n      clip: c.clip,\n      exercise: c.exercise ?? null,\n      tags: c.tags ?? [],\n      tp: s.tp,\n      fp: s.fp,\n      fn: s.fn,\n      precision: s.precision,\n      recall: s.recall,\n    };\n  }\n  return results;\n}\n\n/**\n * The gate. Compare per-clip results against a committed baseline and report\n * every clip whose tp/fp/fn moved in EITHER direction.\n *\n * An improvement failing the build is the point: accepting it means updating\n * the baseline in the same pull request, which makes the diff the review\n * artifact and forces whoever widened the hysteresis gap to say which clips\n * moved and why the trade was worth it.\n *\n * @returns {{ok: boolean, changed: Array<object>, missing: string[], added: string[]}}\n */\nexport function compareToBaseline(results, baseline) {\n  const changed = [];\n  const missing = [];\n  const added = [];\n\n  for (const clip of Object.keys(baseline)) {\n    if (!(clip in results)) {\n      missing.push(clip);\n      continue;\n    }\n    const a = baseline[clip];\n    const b = results[clip];\n    const delta = { tp: b.tp - a.tp, fp: b.fp - a.fp, fn: b.fn - a.fn };\n    if (delta.tp || delta.fp || delta.fn) {\n      changed.push({\n        clip,\n        baseline: { tp: a.tp, fp: a.fp, fn: a.fn },\n        current: { tp: b.tp, fp: b.fp, fn: b.fn },\n        delta,\n        // \"regressed\" only in the narrow sense of more errors; the reviewer\n        // still has to look. Both directions fail the gate.\n        regressed: delta.fp > 0 || delta.fn > 0,\n      });\n    }\n  }\n  for (const clip of Object.keys(results)) if (!(clip in baseline)) added.push(clip);\n\n  return { ok: changed.length === 0 && missing.length === 0 && added.length === 0, changed, missing, added };\n}\n\n/**\n * ANTI-PATTERN — DO NOT GATE ON THIS. Exported so a test can demonstrate that\n * it passes on a counter that is wrong twice. A miss and a phantom cancel, the\n * totals agree, and the suite reports coverage it does not have.\n */\nexport function totalsAgreeAntiPattern(truth, predicted) {\n  return truth.length === predicted.length;\n}\n```\n\n## Adapting it\n\n**Thresholds and joint are per exercise, always.** The recipe uses elbow angle with a curl's convention: a large angle is the extended resting position, a small angle is the contraction. A squat is the knee or hip angle; a jumping jack is usually a keypoint position rather than a joint angle at all. Where an exercise runs the other way round, negate the signal before pushing it — the state machine does not care what the number means. Build a library of small per-exercise definitions rather than one universal detector, and expect that to be ongoing work. [Adding rep counting](/guides/add-rep-counting) covers wiring the joint selection to a real camera feed.\n\n**Calibrate to the person.** Fixed absolute thresholds that work for one body may never trigger for another. Capturing a reference rep at the start of a set, or adapting the thresholds to the observed range, is the usual fix; the state machine takes its two thresholds at construction time so you can build a new counter per set once you know the user's range.\n\n**Smooth harder, or differently.** The EMA here is the cheapest thing that works and it introduces systematic lag, which the end-to-end test measures at about 52 ms on a two-second rep. If your signal is noisier than the EMA can handle, a One Euro filter is the common next step; keep the seam, because the counter only ever sees the smoothed value. Signal quality upstream is [pose estimation accuracy](/motion/pose-estimation-accuracy), and when the corpus goes red, check that layer before you touch a threshold.\n\n**Feed it recorded keypoints.** Every frame carries its own timestamp and nothing reads a clock, so a captured keypoint sequence replays deterministically and a whole corpus scores in milliseconds. Replaying real recordings is legitimate and cheap. Generating the keypoints from the same pose model you are testing is not a test at all — it scores your state machine against your own model's opinion, and the corpus exists to catch the cases where that opinion is wrong.\n\n**Gate the build on the per-clip diff.** `compareToBaseline` fails on any clip whose true positives, false positives or false negatives moved in either direction, including improvements. Failing on an improvement is deliberate: accepting it means updating the committed baseline in the same pull request, which makes the diff the review artifact and forces whoever widened the hysteresis gap to say which clips moved and why the trade was worth it.\n\n## The test contract\n\nThe suite in `cookbook/rep-counter.test.mjs` asserts:\n\n- **A clean sinusoid counts exactly N**, for N of 1, 5, 8 and 12, with zero rejected cycles and rep events spaced one period apart.\n- **Jitter parked on a threshold counts once.** With smoothing disabled so the raw noise reaches the state machine, a signal that crosses 60 degrees eight times and 140 degrees six times produces exactly one rep.\n- **An abandoned half-rep counts zero**, in both directions: a descent that never reaches the low threshold, and a rep that goes all the way down and never fully re-extends. The second case leaves the counter honestly in the up phase rather than guessing.\n- **A twitch is rejected, not delayed.** A full-range spike travelled in 66 ms is recorded as `phase_too_short` and never becomes a count once the debounce elapses.\n- **Low-confidence frames are skipped rather than smoothed in**, so three clean reps stay three when every seventh frame returns a garbage angle.\n- **The scorer catches the compensating-error case.** One miss plus one phantom gives a total that matches the label count exactly — the anti-pattern assertion passes — while precision and recall both come out at 0.75.\n- **Matching is one-to-one**, so a counter that fires twice for one rep produces a false positive rather than a free match.\n- **A tolerance window wider than half the closest labelled rep gap is refused** with a RangeError, before it can score anything.\n- **An empty prediction set scores recall 0 and precision null**, because no evidence is not the same as perfect, and a clip of somebody not exercising scores zero false positives.\n- **The corpus gate fails on a per-clip move the pooled figure hides**: one clip loses a rep, another gains a phantom, the aggregate does not budge, and the gate reports both clips with signed deltas.\n- **Labelling at the wrong moment scores zero.** The same perfect counter, scored against labels placed at the peak instead of the return crossing, gets precision 0 and recall 0 — which is the convention trap, made visible.\n\n## What this does not do\n\nIt does not tell you whether the rep was any good. A counter has one integer and no way to explain itself; judging depth or bar path is [form feedback](/motion/how-form-feedback-works), which has a channel for telling the user why. Our opinion, from the testing page: a shallow rep should usually be counted and flagged rather than silently dropped, because dropping it makes a form problem present to the user as a counting bug and the count is the thing they can check.\n\nIt also produces no comparable accuracy number. Whatever precision and recall your corpus reports describe your clips, your filming and your bodies. Publishing them as an accuracy claim is measuring your own office and calling it a fact about the world."
  },
  {
    "slug": "backfill-checkpointer",
    "primaryQuery": "resumable backfill checkpoint 429 retry code example",
    "h1": "Backfill Checkpointer: A Resumable Window Walker",
    "metaTitle": "Resumable Backfill Walker: Checkpoints and 429s",
    "metaDescription": "Runnable JavaScript for a newest-first backfill: checkpoint after each window, retry the same window on 429, and record gaps instead of skipping.",
    "updated": "2026-08-12",
    "answer": "A copy-and-run implementation of the resumable backfill job: newest-first civil-date windows that widen as they go back, a checkpoint written to an injectable store after each window commits rather than before, 429 and 5xx handled by retrying the same window with jittered exponential backoff and Retry-After as a floor, and an exhausted retry budget recorded as a gap carrying the window and the reason instead of silently advancing. A permission wall is a distinct terminal state that is never retried, and an unrecognised error crashes with the checkpoint intact rather than being laundered into a gap. Plain modern JavaScript, no dependencies, Node 20 and above, with a node:test suite that injects the fetcher, the sleeper, the clock and the store so the whole thing runs with no network and no waiting.",
    "faqs": [
      {
        "q": "Should the checkpoint be written before or after a window commits?",
        "a": "After, always, and the ordering is the only reason this recipe offers any guarantee at all. Fetch, then commit, then checkpoint. Written in that order, a crash anywhere in the sequence leaves at most one window to redo, and the recipe's test suite proves it by failing the checkpoint write immediately after a window has committed and asserting the resumed run re-fetches exactly one window. Written the other way round, a crash between the checkpoint and the commit loses a window permanently and nothing downstream can tell. The price of the safe ordering is that the commit has to be idempotent on the provider's own record identity, because the redo is a re-commit."
      },
      {
        "q": "What should a backfill worker do when its retry budget runs out on a window?",
        "a": "Record the window and why it failed, then move on to the next one. Never mark it done and never let the walk stop. This implementation writes an entry carrying the window id, its civil-date bounds, a reason code, the attempt count and the last error, and it keeps that list in the checkpoint so it survives a restart. The reason the reason code matters is that each one implies a different next action: a retry-exhausted window is worth another pass later, while a window behind a permission wall is not, and collapsing them means either hammering the wall forever or abandoning recoverable history. What you must not do is advance quietly, because an advanced window is indistinguishable from a covered one afterwards."
      },
      {
        "q": "How do I stop a recorded gap from being rendered as a zero-activity day?",
        "a": "Keep the gap list as a first-class input to the display layer rather than as a log line. The walker returns the gaps and persists them, and the coverage report tells you how many days are covered against how many are gapped, which is the number a progress banner should use. Downstream, not imported yet, not permitted to read, and genuinely no activity have to render as three different empty states; a zero in place of the first two is a correctness bug rather than a display choice. Also suppress anything derived from a range that is still incomplete. A partial history produces a confident personal record the user knows they never set, and they remember the number long after the import finishes."
      },
      {
        "q": "Is it safe to run two workers against one checkpoint store?",
        "a": "Not as written, and that is deliberate. This walker owns its job and assumes one runner, because the guarantee it makes is about crash resumption rather than concurrency. To fan out, move claiming into the store: give each window a claimed-by and a claim expiry so a dead worker's window becomes reclaimable, and have workers claim the highest-priority unclaimed window rather than iterating a list. The other separation matters more in practice. Keep the backfill and the live incremental path in different queues with different cursors, and reserve a share of the per-user quota for the live path first, or a slow multi-year import makes today's data stale to complete a year nobody is looking at."
      }
    ],
    "related": [
      {
        "href": "/architecture/historical-backfill",
        "label": "Historical backfill strategy"
      },
      {
        "href": "/test/rate-limits-and-outages",
        "label": "Testing 429s and outages"
      },
      {
        "href": "/architecture/missing-data-and-gaps",
        "label": "Missing data and gaps"
      },
      {
        "href": "/fix/fitbit-api-429-rate-limit",
        "label": "Fitbit 429 rate limits"
      }
    ],
    "cta": {
      "pitch": "Provider quotas move without changelogs, and a backfill is where you find out. Our newsletter tracks what changes for teams importing wearable history, and we publish the harnesses that make the change visible before a user does."
    },
    "body": "A multi-year first sync is a job, not a loop: ordered newest-first, chunked by civil-date window, checkpointed after every chunk, and degrading against a quota rather than discovering one by exhausting it. The design argument is [backfilling years of wearable data](/architecture/historical-backfill), and the fault cases you have to inject to prove it works are [testing 429 and outage handling](/test/rate-limits-and-outages). This page is the walker those two describe.\n\n## What the recipe does\n\n- **Plans windows newest-first, widening backwards.** The default ladder is the last 7 days, then the rest of the last 30, then the rest of the last 90, then a year at a time. Recent windows are small so the first useful screen lands in seconds; old windows are large because nobody is waiting on them and a bigger window costs fewer round trips per day of history covered. Bounds are civil dates, inclusive, never instants — a chunk is \"the user's March\", and chunking on instants makes a traveller's boundaries drift against the days you later render.\n- **Checkpoints after the commit, never before.** The ordering inside one window is fetch, then commit, then checkpoint. A crash anywhere in that sequence costs a re-run of that one window and nothing else, which is why the commit has to be idempotent on the provider's own record identity.\n- **Retries the same window on 429 and 5xx.** Exponential backoff with jitter, and Retry-After treated as a floor rather than a suggestion. RFC 6585 makes the header a MAY, so the no-header path is the normal path, not the exception.\n- **Records a gap when the budget runs out.** The window, its bounds, the reason, the attempt count and the last error. It never silently advances, because an advanced window is indistinguishable from a covered one and there is no later query that can tell them apart.\n- **Treats a permission wall as terminal, not retryable.** A 401 or 403 is recorded once and never re-asked, including on resume. Collapsing \"we were not allowed to read this\" into \"this failed\" is how you end up hammering a wall on a backoff schedule forever.\n- **Rethrows anything it does not recognise.** An unclassified error is a bug on your side, and laundering it into a gap makes a false claim about the provider. It crashes with the checkpoint intact instead.\n\n## The implementation\n\nThis is the canonical file. It lives in the repo at `cookbook/backfill-checkpointer.mjs` and its test file runs in CI on every commit, so what you are reading is what passes.\n\n```js\n/**\n * backfill-checkpointer.mjs\n *\n * WHAT THIS IMPLEMENTS\n *   A resumable historical-backfill walker. Newest-first civil-date windows\n *   over a date range, widening as they go back; a checkpoint written to an\n *   injectable store AFTER each window commits; 429 and 5xx handled by\n *   retrying the SAME window with exponential backoff (honouring Retry-After)\n *   rather than skipping it; a degrade path that records `{ window, reason }`\n *   when the retry budget is exhausted instead of silently advancing; and a\n *   resume that re-does at most the one window that was in flight.\n *\n *   A permission wall is a distinct terminal state, not a retryable failure —\n *   collapsing the two is how you hammer a wall on a backoff schedule forever.\n *   Anything unrecognised is fatal and rethrown, because an unexpected bug\n *   should crash loudly, not be laundered into a gap.\n *\n * WHICH aifitnessapi.com PAGES DOCUMENT THE PATTERN\n *   https://aifitnessapi.com/architecture/historical-backfill\n *   https://aifitnessapi.com/test/rate-limits-and-outages\n *   https://aifitnessapi.com/cookbook/backfill-checkpointer\n *\n * Node 20+. Zero runtime dependencies. The clock, the sleeper, the jitter\n * source, the checkpoint store and the fetcher are all injected, so the whole\n * thing runs in microseconds with no network and no real waiting.\n *\n * MIT — from aifitnessapi.com/cookbook\n */\n\nconst MS_PER_DAY = 86_400_000;\n\n/** Why a window ended up as a gap. Each implies a different next action. */\nexport const GAP_REASON = Object.freeze({\n  RETRY_EXHAUSTED: \"retry_exhausted\",\n  BLOCKED_BY_PERMISSION: \"blocked_by_permission\",\n  BLOCKED_BY_PROVIDER_CAP: \"blocked_by_provider_cap\",\n});\n\n// ---------------------------------------------------------------------------\n// Window planning — civil dates, newest first, widening backwards\n// ---------------------------------------------------------------------------\n\nfunction parseCivilDate(s) {\n  const m = /^(\\d{4})-(\\d{2})-(\\d{2})$/.exec(s);\n  if (!m) throw new TypeError(`not a YYYY-MM-DD civil date: ${s}`);\n  return Date.UTC(Number(m[1]), Number(m[2]) - 1, Number(m[3]));\n}\n\nfunction formatCivilDate(ms) {\n  const d = new Date(ms);\n  return `${d.getUTCFullYear()}-${String(d.getUTCMonth() + 1).padStart(2, \"0\")}-${String(\n    d.getUTCDate(),\n  ).padStart(2, \"0\")}`;\n}\n\n/**\n * Plan the windows for a backfill, newest first.\n *\n * `lookbackLadder` is a list of cumulative lookbacks in days: the default\n * [7, 30, 90] produces \"the last 7 days\", then \"the 23 days before that\", then\n * \"the 60 days before that\", after which windows are `thenEveryDays` long all\n * the way back to `from`.\n *\n * Recent windows are small so the first useful screen lands fast; old windows\n * are large because nobody is waiting on them and a bigger window costs fewer\n * round trips per day of history. Bounds are civil dates, inclusive, never\n * instants — a chunk is \"the user's March\".\n *\n * @param {{from: string, to: string, lookbackLadder?: number[], thenEveryDays?: number}} opts\n * @returns {Array<{id: string, start: string, end: string, days: number, priority: number}>}\n */\nexport function planWindows({ from, to, lookbackLadder = [7, 30, 90], thenEveryDays = 365 }) {\n  const fromMs = parseCivilDate(from);\n  const toMs = parseCivilDate(to);\n  if (fromMs > toMs) throw new RangeError(\"`from` must not be after `to`\");\n\n  const windows = [];\n  let cursorEnd = toMs; // inclusive end of the next (older) window\n  let consumed = 0; // days already planned, counting back from `to`\n\n  for (const lookback of lookbackLadder) {\n    if (cursorEnd < fromMs) break;\n    const size = lookback - consumed;\n    if (size <= 0) continue;\n    const start = Math.max(fromMs, cursorEnd - (size - 1) * MS_PER_DAY);\n    windows.push({ start, end: cursorEnd });\n    consumed = lookback;\n    cursorEnd = start - MS_PER_DAY;\n  }\n  while (cursorEnd >= fromMs) {\n    const start = Math.max(fromMs, cursorEnd - (thenEveryDays - 1) * MS_PER_DAY);\n    windows.push({ start, end: cursorEnd });\n    cursorEnd = start - MS_PER_DAY;\n  }\n\n  return windows.map((w, i) => ({\n    id: `${formatCivilDate(w.start)}..${formatCivilDate(w.end)}`,\n    start: formatCivilDate(w.start),\n    end: formatCivilDate(w.end),\n    days: Math.round((w.end - w.start) / MS_PER_DAY) + 1,\n    priority: i, // lower runs first, and index 0 is the most recent window\n  }));\n}\n\n// ---------------------------------------------------------------------------\n// Failure classification\n// ---------------------------------------------------------------------------\n\nconst RETRYABLE_STATUS = new Set([408, 425, 429, 500, 502, 503, 504]);\nconst PERMISSION_STATUS = new Set([401, 403]);\n\nexport function isRetryableStatus(status) {\n  return RETRYABLE_STATUS.has(status);\n}\n\n/**\n * `retry` | `permission` | `provider_cap` | `fatal`.\n *\n * An error is retryable if it says so or carries a retryable status. It is a\n * wall if it says so or carries 401/403. Everything else is a bug in your own\n * code and is rethrown — never turned into a gap, because a gap is a claim\n * about the provider.\n */\nexport function classifyFailure(err) {\n  if (err && err.permanent === true) return err.reason ?? \"permission\";\n  if (err && err.retryable === true) return \"retry\";\n  const status = err && err.status;\n  if (isRetryableStatus(status)) return \"retry\";\n  if (PERMISSION_STATUS.has(status)) return \"permission\";\n  return \"fatal\";\n}\n\n/** Retry-After in milliseconds, if the error carries one. RFC 6585 makes it a MAY. */\nexport function retryAfterMs(err) {\n  if (!err) return null;\n  if (Number.isFinite(err.retryAfterMs)) return err.retryAfterMs;\n  if (Number.isFinite(err.retryAfterSeconds)) return err.retryAfterSeconds * 1000;\n  return null;\n}\n\n/**\n * Exponential backoff with full jitter. `random` is injected so a test gets a\n * deterministic schedule; jitter is not optional in production, because fixed\n * backoff passes every single-user test and then synchronises every worker\n * onto the same second the moment the provider recovers.\n */\nexport function exponentialBackoff({\n  baseMs = 1000,\n  factor = 2,\n  maxMs = 60_000,\n  jitter = 1,\n  random = Math.random,\n} = {}) {\n  return function delayFor(attempt, err) {\n    const ceiling = Math.min(maxMs, baseMs * factor ** Math.max(0, attempt - 1));\n    const jittered = ceiling * (1 - jitter) + ceiling * jitter * random();\n    // Retry-After is a floor, not a suggestion. Never sleep less than it.\n    return Math.max(jittered, retryAfterMs(err) ?? 0);\n  };\n}\n\n// ---------------------------------------------------------------------------\n// Checkpoint store\n// ---------------------------------------------------------------------------\n\n/**\n * The shape a checkpoint store must implement. Swap for a row in Postgres,\n * a KV entry, anything durable — the walker only calls load() and save().\n */\nexport function createMemoryCheckpointStore(seed = {}) {\n  const data = new Map(Object.entries(seed));\n  let saves = 0;\n  return {\n    get saveCount() {\n      return saves;\n    },\n    async load(jobId) {\n      const v = data.get(jobId);\n      return v ? structuredClone(v) : null;\n    },\n    async save(jobId, state) {\n      saves += 1;\n      data.set(jobId, structuredClone(state));\n    },\n  };\n}\n\nfunction emptyCheckpoint() {\n  return { completed: [], gaps: [] };\n}\n\n// ---------------------------------------------------------------------------\n// The walker\n// ---------------------------------------------------------------------------\n\nconst realSleep = (ms) => new Promise((r) => setTimeout(r, ms));\n\n/**\n * Walk the windows newest-first, committing and checkpointing as it goes.\n *\n * Ordering inside one window is load-bearing:\n *   fetch -> commit -> checkpoint.\n * The checkpoint is written last, so a crash anywhere in that sequence costs\n * you a re-run of that one window and nothing else. `commit` must therefore be\n * idempotent on the provider's own record identity; at-least-once is the only\n * delivery a resumable job can offer.\n *\n * @param {object} opts\n * @param {string}   opts.jobId\n * @param {Array}    opts.windows        from planWindows()\n * @param {Function} opts.fetchWindow    async (window) => records\n * @param {Function} [opts.commit]       async (window, records) => void; must be idempotent\n * @param {object}   opts.store          { load(jobId), save(jobId, state) }\n * @param {number}   [opts.maxAttempts=4]  attempts per window, including the first\n * @param {Function} [opts.backoff]      (attempt, err) => ms\n * @param {Function} [opts.sleep]        async (ms) => void\n * @param {Function} [opts.now]          () => epoch ms\n * @param {Function} [opts.onEvent]      (event) => void, for logging and tests\n */\nexport async function runBackfill({\n  jobId,\n  windows,\n  fetchWindow,\n  commit = async () => {},\n  store,\n  maxAttempts = 4,\n  backoff = exponentialBackoff(),\n  sleep = realSleep,\n  now = Date.now,\n  onEvent = () => {},\n}) {\n  if (!jobId) throw new TypeError(\"jobId is required\");\n  if (!store || typeof store.load !== \"function\" || typeof store.save !== \"function\") {\n    throw new TypeError(\"store must implement load(jobId) and save(jobId, state)\");\n  }\n\n  const checkpoint = (await store.load(jobId)) ?? emptyCheckpoint();\n  const completed = new Set(checkpoint.completed);\n  const gaps = checkpoint.gaps.slice();\n  const gapped = new Set(gaps.map((g) => g.window));\n\n  const fetched = [];\n  let slept = 0;\n  let redone = 0;\n\n  for (const window of windows) {\n    if (completed.has(window.id)) {\n      onEvent({ type: \"skip\", window: window.id, reason: \"already_committed\" });\n      continue;\n    }\n    if (gapped.has(window.id)) {\n      onEvent({ type: \"skip\", window: window.id, reason: \"already_gapped\" });\n      continue;\n    }\n\n    let attempt = 0;\n    let outcome = null;\n\n    // Retry loop. Every path out of it is terminal for this window: either it\n    // commits, or it becomes a recorded gap, or it throws.\n    for (;;) {\n      attempt += 1;\n      let records;\n      try {\n        fetched.push(window.id);\n        records = await fetchWindow(window, { attempt });\n      } catch (err) {\n        const kind = classifyFailure(err);\n\n        if (kind === \"retry\") {\n          if (attempt >= maxAttempts) {\n            // Never silently advance. Record what we could not read and why.\n            outcome = {\n              window: window.id,\n              start: window.start,\n              end: window.end,\n              reason: GAP_REASON.RETRY_EXHAUSTED,\n              attempts: attempt,\n              lastError: String((err && err.message) || err),\n              at: now(),\n            };\n            onEvent({ type: \"gap\", ...outcome });\n            break;\n          }\n          const delay = backoff(attempt, err);\n          slept += delay;\n          onEvent({ type: \"retry\", window: window.id, attempt, delay });\n          await sleep(delay);\n          continue; // the SAME window, not the next one\n        }\n\n        if (kind === \"permission\" || kind === \"provider_cap\") {\n          // A wall, not a failure. Retrying it on a backoff schedule forever is\n          // the bug this branch exists to prevent.\n          outcome = {\n            window: window.id,\n            start: window.start,\n            end: window.end,\n            reason:\n              kind === \"permission\"\n                ? GAP_REASON.BLOCKED_BY_PERMISSION\n                : GAP_REASON.BLOCKED_BY_PROVIDER_CAP,\n            attempts: attempt,\n            lastError: String((err && err.message) || err),\n            at: now(),\n          };\n          onEvent({ type: \"gap\", ...outcome });\n          break;\n        }\n\n        // Unclassified: a bug on our side. Crash with the checkpoint intact.\n        onEvent({ type: \"fatal\", window: window.id, attempt });\n        throw err;\n      }\n\n      await commit(window, records);\n      if (attempt > 1) redone += 1;\n      completed.add(window.id);\n      onEvent({ type: \"commit\", window: window.id, attempts: attempt, records: records?.length ?? 0 });\n      outcome = null;\n      break;\n    }\n\n    if (outcome) {\n      gaps.push(outcome);\n      gapped.add(window.id);\n    }\n\n    // Checkpoint AFTER the window is terminal, never before.\n    await store.save(jobId, { completed: [...completed], gaps });\n  }\n\n  return {\n    jobId,\n    committed: [...completed],\n    gaps,\n    fetchedWindows: fetched,\n    requestCount: fetched.length,\n    retriedWithinWindow: redone,\n    sleptMs: slept,\n  };\n}\n\n/**\n * Invariant 1 from /test/rate-limits-and-outages: every window in the requested\n * range ends in a terminal state, and none silently disappeared.\n */\nexport function coverageReport(windows, result) {\n  const done = new Set(result.committed);\n  const gapped = new Set(result.gaps.map((g) => g.window));\n  const missing = windows.filter((w) => !done.has(w.id) && !gapped.has(w.id)).map((w) => w.id);\n  const daysCovered = windows.filter((w) => done.has(w.id)).reduce((n, w) => n + w.days, 0);\n  const daysGapped = windows.filter((w) => gapped.has(w.id)).reduce((n, w) => n + w.days, 0);\n  return {\n    total: windows.length,\n    done: windows.filter((w) => done.has(w.id)).length,\n    gapped: windows.filter((w) => gapped.has(w.id)).length,\n    missing,\n    complete: missing.length === 0,\n    daysCovered,\n    daysGapped,\n  };\n}\n```\n\n## Adapting it\n\n**The checkpoint store is the only thing you must replace.** It needs `load` and `save` and nothing else, so a row in Postgres, a KV entry or a document all fit. Keep the durability honest: the recipe's guarantee is that a resume re-does at most the window that was in flight, and that guarantee is exactly as strong as your store's write.\n\n**Add the provider's pagination cursor if it has one.** The walker checkpoints per window, which is the right granularity for a date-windowed REST API. Where the provider pages inside a window, carry the page token in the checkpoint alongside the window id and resume from it, so a storm mid-window costs a page rather than a chunk. On an anchor-paginated platform the anchor is itself the checkpoint and you do not need date windows at all — see [incremental sync](/architecture/incremental-sync) for what that cursor is and is not.\n\n**Do not tune the ceiling; parameterise it.** There is no published number to tune a Health Connect loop against, and cloud providers that do publish one usually count it per consented user rather than per app, so more workers buy nothing. Run your fake at a ceiling low enough that the storm case triggers every time and assert degradation behaviour rather than throughput. A job that is correct at ten requests an hour is correct at a thousand; the reverse is not true. The symptom-level version for one provider is [Fitbit 429 rate limits](/fix/fitbit-api-429-rate-limit).\n\n**Reserve quota for the live path before the backfill claims any.** The walker takes no view on this, and it should: a backfill that spends the whole per-user allowance makes today's data stale in order to complete a year nobody is looking at. Gate `fetchWindow` behind a budget that the incremental worker draws from first.\n\n**A gap is not a zero.** The gap list is the input to your empty-state logic, and \"not imported yet\", \"not permitted to read\" and \"genuinely no activity\" are three different facts that must render as three different things. Rendering the first two as zero is a correctness bug, not a display choice; [missing data and gaps](/architecture/missing-data-and-gaps) is the schema that keeps them apart.\n\n**Suppress derived numbers while it runs.** A partial history is a complete history as far as a personal-record calculation is concerned. Streaks, best-ever markers and trend badges stay off until the range they depend on is covered, because a wrong PR is a number the user will remember long after the import finishes.\n\n## The test contract\n\nThe suite in `cookbook/backfill-checkpointer.test.mjs` asserts:\n\n- **Windows are civil dates, newest first, contiguous and non-overlapping**, with sizes 7, 23, 60 then 365 days, and the planned days summing to the requested range exactly once.\n- **Failure classification separates retry, wall and bug**, and Retry-After acts as a floor: 100 ms of computed backoff becomes 5,000 ms when the provider asks for five seconds, and stays at 800 ms when our own backoff already exceeds what it asked for.\n- **A 429 retries the same window.** The request log reads window 0, window 0, window 0, then window 1 — never window 1 in place of window 0 — with 100 ms and 200 ms of backoff between the attempts.\n- **Exhausted retries record a gap and the walk continues.** The gap carries the window, its bounds, the reason and an attempt count of 3; the window is not in the committed list; the next window is still fetched; and the coverage report shows nothing missing, with the gapped days counted separately from the covered ones.\n- **A permission wall is asked once and never retried**, with no backoff at all, and a resumed run issues zero requests rather than re-asking.\n- **A crash mid-run resumes from the checkpoint.** An unclassified error rejects the run instead of becoming a silent gap; the durable checkpoint holds exactly the two committed windows; and the second run issues zero requests for either of them while every record still lands exactly once.\n- **A crash between commit and checkpoint re-does at most the in-flight window.** With the checkpoint write failing after the second window committed, the resumed run re-fetches exactly one window, commits it a second time, and the idempotent sink still holds one copy of every record.\n- **A completed run's gap list is exact** — one retry-exhausted window and one permission-walled window, in order, with no invented gaps and none swallowed. Every window in the range is terminal, the gap list survives a restart, and the restart issues no requests.\n\n## What this does not fix\n\nIt does not make a backfill fast. It makes it interruptible, observable and non-destructive; a single user's history still arrives at whatever rate their provider's per-user quota allows, and you should size the product experience around hours rather than seconds.\n\nIt also cannot recover what the platform will not hand over. Where a mobile health platform resets its readable window on reinstall, the history you never read is gone, and your own server copy is the only mitigation — which is an argument for backfilling early and completely rather than lazily.\n\nAnd fault injection tells you nothing about how the real provider enforces its limit: whether the bucket is per user or per app, fixed or sliding, or whether repeated violations compound. None of that is in your fake because none of it is documented. What replaces it is one deliberate staging run against a real consented account, plus a permanent production signal on completion time, 429 rate and windows sitting in a gap state longer than they should."
  }
];
