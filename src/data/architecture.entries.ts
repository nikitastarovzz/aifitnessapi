import type { ClusterEntry } from "@/lib/cluster";

/** AUTO-ASSEMBLED architecture pages (regenerate via scratchpad/assemble15.mjs).
 *  Placeholder until the cluster is written. */
export const architectureEntries: ClusterEntry[] = [];
