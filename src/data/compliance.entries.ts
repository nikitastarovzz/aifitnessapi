import type { ClusterEntry } from "@/lib/cluster";

/**
 * AUTO-ASSEMBLED health-data compliance & privacy pages (do not hand-edit;
 * regenerate via scratchpad/assemble8.mjs). Article + FAQPage (no HowTo).
 * General engineering guidance, not legal advice — see ClusterDisclaimer legal
 * variant. Placeholder until the writers land.
 */
export const complianceEntries: ClusterEntry[] = [];
