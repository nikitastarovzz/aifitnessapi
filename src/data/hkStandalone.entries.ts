import type { HkStandaloneEntry } from "./hkStandalone";

/**
 * Authored synthesis for the standalone HealthKit reference pages.
 * Writing pass of 2026-09-04; every number verified against the dataset,
 * HKUnit quotes fetched from Apple's documentation 2026-09-04.
 */
export const hkStandaloneEntries: HkStandaloneEntry[] =
[
  {
    "slug": "healthkit-versions",
    "title": "HealthKit Types by iOS Version",
    "metaDescription": "127 of 240 HealthKit type identifiers shipped in iOS 8.0. The rest arrived in bursts: running in 16, cycling in 17, rowing and effort in 18.",
    "primaryQuery": "healthkit types by ios version",
    "intro": "127 of the 240 HealthKit type identifiers in this dataset were introduced in\niOS 8.0. More than half of the API surface you will ever query has been there\nsince HealthKit shipped, and the histogram on this page shows what happened\nafter.\n\nWhat happened after is bursts, not a stream. Some releases add a single\nidentifier; others land an entire subsystem at once. iOS 10.0 added 20. iOS\n14.0 added 14, with three more in 14.3 and one each in 14.2 and 14.5. iOS 9.0\nadded 10, iOS 11.0 nine, iOS 13.0 seven, iOS 15.0 five, iOS 12.2 three, and\niOS 9.3 and 11.2 one apiece. Then the three modern bursts.\n\n**iOS 16.0 added 17**, the largest of the recent ones, and it is three\nunrelated projects arriving together. Running dynamics accounts for five:\n`runningGroundContactTime`, `runningPower`, `runningSpeed`,\n`runningStrideLength` and `runningVerticalOscillation`. A sleep-time body\nsignal accounts for one, `appleSleepingWristTemperature`, a discrete\ntemperature quantity that carries one of the longest discussion sections\nApple has written for any identifier. Menstrual-cycle deviations account for\nfour: `infrequentMenstrualCycles`, `irregularMenstrualCycles`,\n`persistentIntermenstrualBleeding` and `prolongedMenstrualPeriods`. The rest\nare `atrialFibrillationBurden`, `heartRateRecoveryOneMinute`,\n`environmentalSoundReduction`, `underwaterDepth`, `waterTemperature`, and two\nworkout activity constants, `swimBikeRun` and `transition`.\n\n**iOS 17.0 added seven**, and four of them are cycling: `cyclingCadence`,\n`cyclingFunctionalThresholdPower`, `cyclingPower` and `cyclingSpeed`. The\nother three are `timeInDaylight` (a cumulative time quantity),\n`physicalEffort`, and the `underwaterDiving` workout constant.\n\n**iOS 18.0 added ten**, and the theme is boats, boards and skis:\n`distanceRowing`, `rowingSpeed`, `distancePaddleSports`, `paddleSportsSpeed`,\n`distanceCrossCountrySkiing`, `crossCountrySkiingSpeed` and\n`distanceSkatingSports`. Alongside them arrived the two effort scores,\n`workoutEffortScore` and `estimatedWorkoutEffortScore`, plus\n`appleSleepingBreathingDisturbances`.\n\n**iOS 26.2 added one**: `hypertensionEvent`, a category type that is listed\nwith no abstract at all.\n\n**iOS 27.0 added two**, both in reproductive health and both in beta:\n`bleedingAfterMenopause` and `menopausalState`.\n\nThe lead time is the part worth planning around. An identifier's introduction\nversion is the floor under any feature that reads it — a build that targets\nolder devices can compile against a newer type and still find it unavailable\nat runtime on the phone in a user's pocket. Reading a running-power chart\nmeans iOS 16.0 or later for that chart specifically, not for your app. The\npractical pattern is per-feature availability checks rather than a single\nraised deployment target, because the 127 identifiers from iOS 8.0 cover\nsteps, distance, energy, heart rate, body mass and most of nutrition, and\nthose work everywhere.\n\nThe second thing the timeline tells you is where Apple is currently investing.\nEvery burst since iOS 16.0 has been a sport-specific measurement set —\nrunning, then cycling, then rowing and paddling and skiing — plus one or two\nhealth-event category types. If you are building for a sport Apple has not\nreached yet, the generic types are what you have, and they will stay what you\nhave until a release adds the specific ones.\n\nThe counts and membership lists here come from this site's [HealthKit\nidentifier dataset](/healthkit-identifiers), parsed from Apple's developer\ndocumentation on 2026-08-28. Whether a given identifier is safe to build on —\nbeta, undocumented, or sitting in a group Apple named \"deprecated\" — is a\nseparate question answered on [HealthKit type\nstatus](/healthkit-status). For what the newest arrivals suggest about the\nnext ones, see [the newest types as a\nroadmap](/blog/healthkit-newest-types-roadmap); for the iOS 8.0 majority, see\n[240 types, most of them from iOS\n8](/blog/healthkit-240-types-ios-8). Setup and entitlements live in\n[integrate HealthKit](/integrate/healthkit).",
    "traps": "**The newest identifiers are the thinnest.** The iOS 18.0 burst brought\n`workoutEffortScore`, `estimatedWorkoutEffortScore` and\n`appleSleepingBreathingDisturbances`, and all three are listed with no\nabstract at all, no aggregation style, and no unit family. `hypertensionEvent`\nfrom iOS 26.2 is listed with no abstract either, and no value enum resolved\nfor it. Arriving in a release tells you the symbol exists. It does not tell\nyou what to put in it or how to roll it up, and for these four Apple has not\nwritten that down yet.\n\n**Two of the newest are in beta.** `bleedingAfterMenopause` and\n`menopausalState`, both iOS 27.0, are marked beta on at least one platform.\nBeta symbols can be renamed, respecified, or have their value sets changed\nbefore general release, so anything you persist keyed to them is a migration\nwaiting to happen.\n\n**Introduced is not populated.** A version number says when the type became\naddressable, not when data started appearing in it. Whether samples exist\ndepends on the hardware the user owns and whether any app writes them —\nblood oxygen is the standard example, since availability has been subject to\nlitigation in the US and needs checking against current status rather than\nagainst an availability table.\n\n**Old is not the same as documented.** Age buys stability, not prose. 58\nidentifiers across the whole set carry zero words of discussion, and the\nmedian discussion length is 15 words, so plenty of the iOS 8.0 cohort is as\nsilent as anything shipped last year.",
    "faqs": [
      {
        "q": "Which iOS version introduced the most HealthKit types?",
        "a": "iOS 8.0, by a wide margin. It introduced 127 of the 240 identifiers in this dataset, which is the whole original surface: steps, distance, energy, heart rate, body mass and almost all of nutrition. The next largest single release is iOS 10.0 with 20, then iOS 16.0 with 17, then iOS 14.0 with 14."
      },
      {
        "q": "What HealthKit types were added in iOS 18?",
        "a": "Ten. Seven of them are rowing, paddle sports, cross country skiing and skating measurements: distanceRowing, rowingSpeed, distancePaddleSports, paddleSportsSpeed, distanceCrossCountrySkiing, crossCountrySkiingSpeed and distanceSkatingSports. The other three are workoutEffortScore, estimatedWorkoutEffortScore and appleSleepingBreathingDisturbances, and none of those three carries an abstract, a unit family or a stated aggregation style in Apple's documentation."
      },
      {
        "q": "Do newer HealthKit types force me to raise my deployment target?",
        "a": "Only for the feature that reads them. The introduction version is a floor for that identifier, not for your app, so the usual pattern is an availability check around the specific chart or metric. The 127 identifiers from iOS 8.0 cover most of what a general fitness app reads, and those are available everywhere."
      }
    ]
  },
  {
    "slug": "healthkit-status",
    "title": "HealthKit Deprecated and Beta Types",
    "metaDescription": "0 of 240 HealthKit identifiers are flagged deprecated, yet Apple groups 3 under \"Deprecated activity types\". Plus the 2 betas and 4 with no abstract.",
    "primaryQuery": "healthkit deprecated types",
    "intro": "Zero of the 240 HealthKit type identifiers in this dataset are marked\ndeprecated. Not one carries a deprecation flag on any of its platform\navailability entries, and every identifier is listed as available on all six\nplatforms Apple documents: iOS, iPadOS, Mac Catalyst, macOS, visionOS and\nwatchOS. There is no watchOS-only type and no iOS-only type in the set.\n\nThat is the good news, and it is narrower than it sounds. \"Is X deprecated?\"\nis only one of four questions you should be asking before you build on an\nidentifier, and the other three have less comfortable answers.\n\n**Apple's own grouping contains a bucket named \"Deprecated activity types.\"**\nIt holds three workout activity constants — `dance`, `danceInspiredTraining`\nand `mixedMetabolicCardioTraining` — all three introduced in iOS 8.0, all\nthree still listed as available on all six platforms, and none of them\ncarrying a deprecation flag. So the platform metadata and the group name\ndisagree, and this page reports both rather than picking one. Apple's abstract\nfor `danceInspiredTraining` still reads \"The constant for workouts inspired by\ndance, including Pilates, Barre, and Feldenkrais,\" which tells you what it\ncovers and nothing about whether you should reach for it. Treat the group name\nas the signal about intent and the missing flag as the signal about\ncompilation: the symbols work, and Apple has grouped them under a heading\nsaying it would rather you did not use them.\n\n**Two identifiers are in beta on some platform**, and they are the two newest\nin the set: `bleedingAfterMenopause` and `menopausalState`, both category\ntypes introduced in iOS 27.0. `menopausalState` resolves to its own value\nenum, `HKCategoryValueMenopausalState`. `bleedingAfterMenopause` does not\nresolve to one at all in this dataset — it is one of only two category types\nwithout a resolved value enum. Beta symbols are the one part of the set where\nthe shape of the data can still change under you.\n\n**Four identifiers are listed with no abstract at all**:\n`appleSleepingBreathingDisturbances`, `estimatedWorkoutEffortScore`,\n`workoutEffortScore` and `hypertensionEvent`. The first three are quantity\ntypes that also have no stated aggregation style and no unit family, which\nmeans there is no documented answer to what they measure, in what, or whether\nyou may add them together. `hypertensionEvent` is a category type from iOS\n26.2 with no abstract, no discussion and no resolved value enum.\n\n**58 identifiers carry zero words of discussion prose**, and the median\ndiscussion length across the whole set is 15 words. Silence is not a lifecycle\nstatus, but it has the same practical effect: an undocumented type is one you\ncannot verify your assumptions against, and assumptions about health data\ntend to be wrong in ways that never throw.\n\nSo the honest four-part answer to \"is X safe to build on\" is: nothing is\nflagged deprecated; three activity constants sit in a group Apple named\ndeprecated; two are beta; and a much larger population is simply\nunderdocumented. The counts come from this site's [HealthKit identifier\ndataset](/healthkit-identifiers), read from Apple's developer documentation on\n2026-08-28. Which release each identifier arrived in is on [HealthKit types by\niOS version](/healthkit-versions). For what the silent ones cost you in\npractice, see [the 58 silent types](/blog/healthkit-58-silent-types), and for\nhow these judgements are made, [how we\nverify](/blog/how-we-verify). If a type you depend on does go away, [migrating\noff a deprecated API](/migrate/migrate-off-a-deprecated-fitness-api) covers\nthe mechanics.",
    "traps": "**Do not read the zero as an endorsement.** No deprecation flag across 240\nidentifiers means Apple has not marked any of them, not that Apple has\npromised anything. The three constants in the \"Deprecated activity types\"\ngroup are the proof: a symbol can be signposted as unwanted in the\ndocumentation's own structure while the availability metadata stays clean, and\na compiler warning is not going to tell you about it.\n\n**Deprecation also happens below the type.** A type can be perfectly current\nwhile one of its enum values is not. Apple's `.asleep` sleep value is\ndeprecated in favour of the specific stages, so an integration reading\n`sleepAnalysis` can be building on a deprecated value inside a\nnon-deprecated type. A type-level status check will never surface that; only\nreading the value enum's own page will.\n\n**Beta means the schema is not settled.** `bleedingAfterMenopause` and\n`menopausalState` are iOS 27.0 betas, and `bleedingAfterMenopause` has no\nvalue enum resolved in this dataset at all. Writing samples of either into\nyour own storage, keyed by a raw integer value, bets that the value set will\nnot change before general release. If you must handle them now, keep the raw\nsample and re-derive your interpretation later rather than persisting the\ninterpretation.\n\n**Undocumented is the largest risk category, and it has no flag.** Four types\nhave no abstract and 58 have no discussion. Nothing in the API distinguishes\nthem from a well-documented type at call time, so the guard has to be yours:\nverify against Apple's page for that identifier before you ship a rollup, and\nrecord what you found and when.",
    "faqs": [
      {
        "q": "Are any HealthKit type identifiers deprecated?",
        "a": "None of the 240 carries a deprecation flag on its platform availability entries. Apple's documentation nevertheless groups three workout activity constants under a heading called \"Deprecated activity types\": dance, danceInspiredTraining and mixedMetabolicCardioTraining. The symbols still compile and are still listed on all six platforms, so the flag and the grouping disagree."
      },
      {
        "q": "Which HealthKit identifiers are in beta?",
        "a": "Two, and both are reproductive health category types introduced in iOS 27.0: bleedingAfterMenopause and menopausalState. Beta status means the specification can still change, and bleedingAfterMenopause has no value enum resolved in this dataset. Treat anything you persist from either as provisional until they reach general release."
      },
      {
        "q": "How do I tell whether a HealthKit type is safe to build on?",
        "a": "Check four things rather than one: whether it is flagged deprecated, whether it sits in the deprecated activity group, whether it is beta, and whether Apple actually documented it. That last one catches the most cases here, since four identifiers have no abstract at all and 58 have no discussion prose."
      }
    ]
  },
  {
    "slug": "healthkit-category-values",
    "title": "HKCategoryValue Enums by Type",
    "metaDescription": "28 of HealthKit's 30 category types resolve to a value enum, 2 are honest nulls. What a category sample holds, and why averaging one means nothing.",
    "primaryQuery": "hkcategoryvalue enum reference",
    "intro": "A HealthKit category sample does not hold a measurement. It holds a value\ndrawn from an enumeration, plus a start date and an end date — and that is\nthe entire payload. 30 of the 240 identifiers in this dataset are category\ntypes, and the table on this page pairs each of them with the value enum it\nresolves to. 28 resolve. Two do not.\n\nThe distinction that matters when you write the reading code is between a\ntype-specific enum and the generic one. `sleepAnalysis` resolves to\n`HKCategoryValueSleepAnalysis`, `menstrualFlow` to\n`HKCategoryValueMenstrualFlow`, `appleStandHour` to\n`HKCategoryValueAppleStandHour`, `contraceptive` to\n`HKCategoryValueContraceptive`, `ovulationTestResult` to\n`HKCategoryValueOvulationTestResult`, `lowCardioFitnessEvent` to\n`HKCategoryValueLowCardioFitnessEvent`. Each of those carries real\nalternatives: a sleep sample says which stage, a menstrual flow sample says\nhow heavy, a stand-hour sample says stood or idle. The value is information.\n\nThen there is plain `HKCategoryValue`, which is what a type resolves to when\nits value has nothing to say. `mindfulSession`, `handwashingEvent`,\n`toothbrushingEvent`, `sexualActivity`, `pregnancy`, `lactation` and\n`intermenstrualBleeding` all land here, as do the heart-event types\n`highHeartRateEvent`, `lowHeartRateEvent` and `irregularHeartRhythmEvent`, and\nthe iOS 16.0 cycle-deviation types `irregularMenstrualCycles`,\n`infrequentMenstrualCycles`, `persistentIntermenstrualBleeding` and\n`prolongedMenstrualPeriods`. For these the sample's meaning lives entirely in\nits type and its time range: these are the `.notApplicable` types, and reading\nthe value tells you nothing you did not already know from the query you ran. A\nmindfulSession sample means someone meditated, from here until there. There is\nno scale.\n\nThis is why averaging a category type is not a slightly wrong answer but a\ncategory error. The integer you can pull out of a category sample is an\nordinal — a position in a list of alternatives — and arithmetic on ordinals\nproduces a number that measures nothing. The mean of a week of `menstrualFlow`\nsamples is not a flow. The mean of a month of `sleepAnalysis` values is not a\nsleep quality. What category data supports is counting occurrences, measuring\nelapsed time from the start and end dates, taking the latest value, and\ngrouping by value. If you want a number out of a category type, derive it from\nthe dates or from the count, never from the value.\n\nTwo category types have no value enum resolved in this dataset:\n`bleedingAfterMenopause` and `hypertensionEvent`. Both are null on purpose.\nResolving these took three passes: taking the first `HKCategoryValue*` symbol\nmentioned on the page resolved 29 of 30 and was wrong, because a cross-linked\nsymbol on the pregnancy page would have shipped the wrong enum for a type.\nRequiring an exact name match resolved far fewer and was structurally biased\nagainst every type whose value is `.notApplicable`. The rule that shipped\naccepts either the type's own matching enum name or the generic enum, which\nresolves 28 and leaves two honest nulls. A null here means Apple's page did\nnot state it in a form the parser could stand behind — not that no enum\nexists. That choice, and why guessing the last two would have been worse than\nadmitting the gap, is written up in [building the HealthKit\ndataset](/blog/building-the-healthkit-dataset).\n\nEverything above was parsed from Apple's developer documentation on\n2026-08-28 for the [HealthKit identifier\ndataset](/healthkit-identifiers). Which of these types are beta or otherwise\nunsettled is on [HealthKit type status](/healthkit-status). For the two\ncategory types most products actually read, see [sleep tracking\nAPIs](/data/sleep-tracking-api), [what sleep stages\nare](/learn/what-are-sleep-stages) and the [menstrual cycle\nAPI](/data/menstrual-cycle-api).",
    "traps": "**The value is an Int, and nothing stops you charting it.** A category\nsample's value arrives as an integer, so it will happily flow through the same\nrollup code as a step count. Summing or averaging it compiles, runs, and\nproduces a plausible-looking line. Guard this at the boundary: keep category\nvalues in a column typed as an enum or a string in your own storage, so that\nthe arithmetic is impossible rather than merely wrong.\n\n**Two enums are missing, and inventing them is the failure mode.**\n`bleedingAfterMenopause` and `hypertensionEvent` resolve to nothing here.\n`bleedingAfterMenopause` is a beta type from iOS 27.0 and `hypertensionEvent`\nfrom iOS 26.2 is listed with no abstract and no discussion at all. If you need\neither, read Apple's page for that identifier yourself at the moment you build\nagainst it, and treat any value set you find as current only for that day.\n\n**Generic and specific enums are not interchangeable in a switch.** Code\nwritten against `HKCategoryValue` for a handwashing event and copied to read a\n`menstrualFlow` sample will compile against raw integers and silently\nmis-label every value. Match on the type's own enum, and make the default\nbranch of the switch loud rather than a fallback that stores a zero.\n\n**Value sets change without the type changing.** A category type can stay\ncurrent while a value inside it is retired or added, and nothing about the\nidentifier's own status will tell you. [HealthKit type\nstatus](/healthkit-status) covers where that has already happened.",
    "faqs": [
      {
        "q": "What is a HealthKit category type?",
        "a": "A type whose samples hold a value from an enumeration plus a start and end date, rather than a number and a unit. 30 of the 240 identifiers in this dataset are category types. A sleep sample says which stage, a stand-hour sample says stood or idle, and an event sample often says nothing beyond the fact that it happened between two times."
      },
      {
        "q": "Can I average HealthKit category samples?",
        "a": "No, and the arithmetic will not stop you. The integer in a category sample is a position in a list of alternatives, not a measurement, so its mean has no meaning. Count the samples, measure elapsed time from the start and end dates, take the latest value, or group by value instead."
      },
      {
        "q": "Why do two category types show no value enum?",
        "a": "Because Apple's pages for bleedingAfterMenopause and hypertensionEvent did not state one in a form the parser could stand behind. The dataset stores a null rather than a guess. An earlier rule resolved 29 of 30 by taking the first enum symbol on the page, and it would have shipped the wrong enum for one type."
      }
    ]
  },
  {
    "slug": "healthkit-units",
    "title": "HKUnit Families by Quantity Type",
    "metaDescription": "116 of HealthKit's 120 quantity types resolve to a unit family, and 4 state none. Includes the compound units: count/time, volume/time, volume/mass/time.",
    "primaryQuery": "healthkit unit types",
    "intro": "Every HealthKit quantity sample stores a number and the unit that number is\nin, and the two are inseparable — a stored double without its unit is not a\nweight, a distance or an energy, it is just a double. This page lists the unit\nfamily each quantity type belongs to. 116 of the 120 quantity types in the\ndataset resolve to one. Four do not.\n\nApple's HKUnit documentation, read 2026-09-04, describes the raw material:\n\"The unit class supports most standard SI units (meters, seconds, and grams),\nSI units with prefixes (centimeters, milliseconds and kilograms) and\nequivalent non-SI units (feet, minutes, and pounds).\" Beyond that flat list it\nadds: \"HealthKit also supports creating complex units by mathematically\ncombining existing units.\" The combining is done by explicit methods —\n`unitDivided(by:)` \"Creates a complex unit by dividing the receiving unit by\nanother unit,\" and there is a matching multiply, a raise-to-power, and a\nreciprocal. There is also a string route in, `init(from:)`, which \"Returns the\nunit instance described by the provided string.\"\n\nThat is why the families in the table below are not all simple nouns. A\nheart rate is not a count and not a time; it is `count/time`, a count divided\nby a minute, and the same construction shape covers `respiratoryRate`,\n`restingHeartRate` and `walkingHeartRateAverage`. `vo2Max` is the deepest of\nthem, `volume/mass/time` — a volume of oxygen per unit of body mass per unit of\ntime, three units combined, which no single named unit can express. `peakExpiratoryFlowRate` is `volume/time`. The sport speeds\n(`runningSpeed`, `cyclingSpeed`, `rowingSpeed`, `paddleSportsSpeed`,\n`crossCountrySkiingSpeed`, `walkingSpeed`) are distance over time, and\n`cyclingCadence` is counts per minute.\n\nThe simple families carry most of the population. Mass carries the largest share of\nthe population, because the dietary types all live there — every logged gram of protein,\nsodium or fat — alongside `bodyMass` and `leanBodyMass`. Length holds the\ndistance types, from `distanceWalkingRunning` through `distanceSwimming` to\nthe iOS 18.0 additions `distanceRowing` and `distancePaddleSports`. Energy\nholds `activeEnergyBurned`, `basalEnergyBurned` and `dietaryEnergyConsumed`.\nCount holds the things that are genuinely dimensionless — `stepCount`,\n`flightsClimbed`, `pushCount`, `swimmingStrokeCount`, and `bodyMassIndex`,\nwhich is an index rather than a measurement. Smaller families are more\nspecific than you might expect: `bloodGlucose` is `mass/volume`,\n`electrodermalActivity` is conductance, `insulinDelivery` is international\nunits, the audio types are sound pressure, and blood pressure is pressure.\n\nThe four quantity types with no unit family stated are\n`appleSleepingBreathingDisturbances`, `estimatedWorkoutEffortScore`,\n`physicalEffort` and `workoutEffortScore`. Three of those four also have no\nabstract and no stated aggregation style. `physicalEffort` is the exception:\nApple describes it as \"A quantity sample type that measures the estimated\namount of energy being used to perform a task excluding other factors such as\ntemperature, altitude, or heart rate\" and states discrete aggregation, but\ndoes not name a unit.\n\nOne note on how these values were produced. The families were derived from\nApple's documentation prose, read on 2026-08-28 for the [HealthKit identifier\ndataset](/healthkit-identifiers), and the first version of the parser\nexcluded the \"/\" character — so every compound unit resolved to nothing and\nheart rate rendered with no unit at all. Widening it took the resolved count\nto 116 of 120. Where the source does not state a unit, the field is null\nrather than filled in.\n\nFor the other half of reading a quantity correctly, see [sum or\naverage](/blog/healthkit-sum-or-average). Per-metric detail lives on [heart\nrate](/data/heart-rate-api) and [VO2 max](/data/vo2-max-api), and unit\nhandling across providers is covered in [normalizing wearable\ndata](/architecture/normalize-wearable-data).",
    "traps": "**A family is not a unit.** Knowing that `bodyMass` is in the mass family does\nnot tell you whether the number you are holding is in kilograms, pounds or\nstones — you decide that when you ask the quantity for its value, and the\nsame sample will answer in any compatible unit. Never store a bare double.\nStore the value and the unit string together, or normalize to one unit at the\nboundary and record which one you chose.\n\n**Four types have no unit at all.** `appleSleepingBreathingDisturbances`,\n`estimatedWorkoutEffortScore`, `physicalEffort` and `workoutEffortScore` state\nno unit family. For three of them Apple also states no abstract and no\naggregation style, so a chart of them has no documented axis and no documented\nrollup. `physicalEffort` at least tells you it is discrete. Display raw\nsamples, or probe the behaviour yourself and write down what you found.\n\n**The family labels are prose-derived, so their wording varies.** The same\ndimension can appear as \"percent\" in one row and \"percentage\" in another, or\n\"length\" in one and \"distance\" in another, because each value came from the\nsentence Apple wrote on that identifier's page rather than from a fixed\nvocabulary. Read the family as a hint for a human, not as a join key — if you\nneed to branch on dimension in code, map identifiers to your own enum.\n\n**Compound units are where parsers break.** They broke this one: excluding the\n\"/\" character from the unit pattern silently emptied every `count/time`,\n`volume/time` and `volume/mass/time` row, and the visible symptom was heart\nrate showing no unit rather than an error. If you are extracting units from\ndocumentation, test the compound cases first.",
    "faqs": [
      {
        "q": "Which HealthKit quantity types have no unit?",
        "a": "Four: appleSleepingBreathingDisturbances, estimatedWorkoutEffortScore, physicalEffort and workoutEffortScore. Apple's pages for the first three also carry no abstract and no aggregation style. physicalEffort is described and stated as discrete, but no unit is named for it. The remaining 116 quantity types resolve to a family."
      },
      {
        "q": "How does HealthKit express a unit like beats per minute?",
        "a": "By combining simpler units. Apple's HKUnit documentation, read 2026-09-04, states that HealthKit \"supports creating complex units by mathematically combining existing units\", and unitDivided(by:) \"Creates a complex unit by dividing the receiving unit by another unit\". A heart rate is therefore a count divided by a time, which is why its family reads count/time."
      },
      {
        "q": "Is a unit family the same as the unit I read a sample in?",
        "a": "No. The family tells you the dimension the type measures; the unit is what you ask the quantity for when you pull the value out, and any compatible unit will answer. Two apps reading the same body mass sample can legitimately get different numbers, so record the unit alongside every value you store."
      }
    ]
  },
  {
    "slug": "health-connect-records",
    "title": "Health Connect Records We Verified",
    "metaDescription": "10 metrics verified on both Health Connect and HealthKit, with record names and the structural mismatches. A verified subset, not Google's full catalog.",
    "primaryQuery": "health connect record types",
    "intro": "Start with what this page is not. It is not a catalogue of Health Connect.\nGoogle documents the full set of record types, and that set is larger than\nwhat appears here. This page lists the 10 metrics this site has verified on\nboth platforms — checked against Apple's documentation and Google's, and\nlisted only once both sides were read. Everything else is absent because it\nhas not been checked yet, not because it does not exist.\n\nWith that said, the 10 are the ones a fitness app reaches for first, and the\ninteresting part is not that both platforms have them. It is the shape of the\nmismatch when you try to write one schema over both.\n\n**Heart rate** is `HeartRateRecord` and `RestingHeartRateRecord` against\nApple's `heartRate` and `restingHeartRate`. Both platforms give you samples\nplus a separate resting value, which is a favour — do not derive resting\nyourself.\n\n**HRV** is the one that looks aligned and is not.\n`HeartRateVariabilityRmssdRecord` against `heartRateVariabilitySDNN`. Apple\nstores SDNN, Health Connect stores RMSSD, and they are different measures\nthat are not interconvertible. [SDNN vs RMSSD](/blog/hrv-sdnn-vs-rmssd) is the\nlong version.\n\n**VO2 max** is `Vo2MaxRecord` against `vo2Max` — an estimate on both\nplatforms rather than a lab measurement, with Health Connect additionally\ntagging a measurement method.\n\n**Blood oxygen** is `OxygenSaturationRecord` against `oxygenSaturation`. The\ntype existing does not mean data exists: SpO2 is device-gated, and Apple Watch\navailability in the US has been subject to litigation, so verify current\nstatus rather than assuming.\n\n**Sleep** is the structural mismatch. Apple gives you\n`HKCategoryTypeIdentifier.sleepAnalysis`, a category type whose values are\ninBed, awake, asleepCore, asleepDeep, asleepREM and asleepUnspecified. Health\nConnect gives you `SleepSessionRecord`, a session that carries stages. Stage\nvocabularies differ between them and stages are estimated rather than\nmeasured, so a shared \"sleep quality\" score computed across both platforms is\ncomparing two different estimators.\n\n**Steps** is `StepsRecord` and `StepsCadenceRecord` against `stepCount`, with\nCMPedometer for live counts on iOS. Both sides need de-duplication, since a\nphone and a watch can write the same walk. On Android there is one extra rule:\nfrom the June 2026 update on-device steps are attributed to a per-device\nSynthetic Package Name, which you read via `getCurrentDeviceDataSource()` and\nnever hardcode.\n\n**Workouts** is `ExerciseSessionRecord` and `PlannedExerciseSessionRecord`\nagainst `HKWorkout` and `HKWorkoutBuilder`. The activity-type taxonomies\ndiffer, so map them explicitly rather than by matching names.\n\n**GPS routes** is `ExerciseRoute` with `ExerciseRoute.Location` against\n`HKWorkoutRoute` and `HKWorkoutRouteQuery`. Health Connect gates routes behind\nits own permission, READ_EXERCISE_ROUTES, and restricts background reads of\nother apps' routes.\n\n**Calories** is the asymmetry to watch: `ActiveCaloriesBurnedRecord` and\n`TotalCaloriesBurnedRecord` against `activeEnergyBurned` and\n`basalEnergyBurned`. Apple splits active and basal; Android offers active and\ntotal. Both are modelled estimates.\n\n**Body composition** is one Apple concept against a shelf of records:\n`WeightRecord`, `BodyFatRecord`, `LeanBodyMassRecord`, `BoneMassRecord`,\n`BodyWaterMassRecord` and `BasalMetabolicRateRecord` against `bodyMass`,\n`bodyFatPercentage`, `leanBodyMass` and `bodyMassIndex`. The store holds it,\nbut something has to write it — usually a smart scale or a manual entry.\n\nThe full side-by-side with the watch-outs is the [cross-platform\nmatrix](/matrix). For the platform-level comparison see [HealthKit vs Health\nConnect](/fitness-apis/apple-healthkit-vs-google-health-connect), for setup\n[integrate Health Connect](/integrate/google-health-connect), and for the\nApple side [the HealthKit identifier\ndataset](/healthkit-identifiers).",
    "traps": "**Do not read this list as coverage.** These are the 10 metrics verified\nagainst both vendors' documentation, not the boundary of what Health Connect\nsupports. If a record type you need is missing here, the answer is that we\nhave not checked it, and Google's own documentation is where to look.\n\n**Do not normalize SDNN into RMSSD.** Apple stores heart rate variability as\nSDNN and Health Connect stores it as RMSSD. They are different measures and\nare not interconvertible, so a single \"HRV\" column populated from both\nplatforms holds two incompatible quantities that will trend against each other\nfor reasons that have nothing to do with the user.\n\n**Do not add active calories to total calories.** Apple splits active and\nbasal; Health Connect offers active and total. Those are not the same\nsplit, and the rule for the Android pair is simply that you must not add\nactive to total — the result is a plausible number that nothing will flag.\nBoth figures are modelled estimates on both platforms, not measurements.\n\n**Do not hardcode the steps data source on Android.** From the June 2026\nupdate, on-device steps are attributed to a per-device Synthetic Package Name.\nRead it with `getCurrentDeviceDataSource()`. A literal package name in your\nde-duplication logic will work on the device you tested on and quietly stop\nfiltering on everyone else's — and de-duplication matters here, because a\nphone and a watch both write steps for the same walk.",
    "faqs": [
      {
        "q": "Does this page list every Health Connect record type?",
        "a": "No. It lists the 10 metrics this site has verified against both Apple's and Google's documentation. Google documents the full catalog, which is larger. A record type missing from this page means we have not checked it against both vendors yet, not that Health Connect lacks it."
      },
      {
        "q": "Can I map Apple HRV onto Health Connect HRV?",
        "a": "Not by conversion. Apple's heartRateVariabilitySDNN stores SDNN and Health Connect's HeartRateVariabilityRmssdRecord stores RMSSD. These are different measures of the same underlying signal and are not interconvertible, so keep them in separate fields and label which one any chart is showing."
      },
      {
        "q": "Which Health Connect records match Apple's calorie types?",
        "a": "ActiveCaloriesBurnedRecord and TotalCaloriesBurnedRecord sit opposite Apple's activeEnergyBurned and basalEnergyBurned. The split is not the same on both sides, so map them explicitly and never add the active figure to the total figure. Both platforms are giving you modelled estimates rather than measurements."
      }
    ]
  }
];
