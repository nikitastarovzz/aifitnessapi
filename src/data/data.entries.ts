import type { ClusterEntry } from "@/lib/cluster";

/**
 * AUTO-ASSEMBLED "[metric] API" pages (do not hand-edit; regenerate via
 * scratchpad/assemble12.mjs). Article + FAQPage (no HowTo). Organized by the
 * data a builder needs (heart rate, steps, sleep, …) — which sources expose it,
 * measured vs estimated, and the best pick. Placeholder until the writers land.
 */
export const dataEntries: ClusterEntry[] = [];
