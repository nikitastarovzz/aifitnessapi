import type { ClusterEntry } from "@/lib/cluster";

/**
 * AUTO-ASSEMBLED fitness/health API pricing pages (do not hand-edit; regenerate
 * via scratchpad/assemble10.mjs). Article + FAQPage (no HowTo). Pricing is
 * volatile — every figure is hedged "verify current pricing"; the honest
 * through-line is that most first-party wearable APIs are free to call.
 * Placeholder until the writers land.
 */
export const pricingEntries: ClusterEntry[] = [];
